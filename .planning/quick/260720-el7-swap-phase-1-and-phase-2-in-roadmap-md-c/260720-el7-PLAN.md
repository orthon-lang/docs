---
phase: quick-260720-el7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md]
autonomous: true
requirements: [PROC-01, PROC-02, PROC-03, PROC-04, VISION-01, VISION-02, VISION-03, DEBT-01, DEBT-02, DEBT-03, DEBT-04, DEBT-05, DEBT-06, DEBT-07, DEBT-08, DEBT-09, DEBT-10, DEBT-11, DEBT-12, DEBT-13, DEBT-14, DEBT-15, DEBT-16, DEBT-17]
user_setup: []

must_haves:
  truths:
    - "ROADMAP.md Phase 1 is titled 'Concerns Remediation' and physically appears before Phase 2, titled 'Foundations, Process & Vision', in the Phase Details section"
    - "ROADMAP.md new Phase 1 (Concerns Remediation) depends on nothing; new Phase 2 (Foundations, Process & Vision) depends on Phase 1"
    - "ROADMAP.md Phase 3's Depends on line references Phase 2, not Phase 1, since PROC-01/02 (the acceptance gate) now live in Phase 2"
    - "REQUIREMENTS.md traceability table maps DEBT-01..17 to Phase 1 and PROC-01..04 / VISION-01..03 to Phase 2"
    - "STATE.md Current focus and Current Position both read Phase 1, Concerns Remediation"
    - "STATE.md Blockers/Concerns bullets cite the correct new phase numbers for PROC-01/02 (Phase 2) and DEBT-01..04 (Phase 1)"
  artifacts:
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "ROADMAP.md '### Phase 1: Concerns Remediation' Depends on line reads '**Depends on**: Nothing (first phase)'"
    - "ROADMAP.md '### Phase 2: Foundations, Process & Vision' Depends on line reads '**Depends on**: Phase 1'"
    - "ROADMAP.md '### Phase 3: Language Inventory...' Depends on line reads '**Depends on**: Phase 2 (requires the acceptance gate and review process from PROC-01/02)'"
    - "REQUIREMENTS.md Traceability table rows for DEBT-01..17 read 'Phase 1'; rows for PROC-01..04 and VISION-01..03 read 'Phase 2'"
    - "REQUIREMENTS.md Phase summary bullets list Phase 1 (Concerns Remediation) before Phase 2 (Foundations, Process & Vision)"
---

<objective>
Swap the execution order of Phase 1 and Phase 2 in `.planning/ROADMAP.md`: the phase
currently numbered 2 ("Concerns Remediation", the tech-debt/architecture-spec
leftovers) becomes the new Phase 1, and the phase currently numbered 1 ("Foundations,
Process & Vision") becomes the new Phase 2, now depending on the new Phase 1. Phases 3,
4, and 5 keep their numbers and content, with only Phase 3's Depends-on reference
updated to point at the renumbered Phase 2. Propagate the renumbering into
`.planning/REQUIREMENTS.md` (traceability table + phase summary) and
`.planning/STATE.md` (current focus/position plus the Decisions and Blockers/Concerns
bullets that cite phase numbers by name).

Purpose: The user explicitly requested this reordering: leftovers (tech-debt
remediation) should run first, foundations/process/vision work should run second,
overriding the prior sequencing decision recorded in STATE.md. Neither phase has
started (both show "Not started", 0/TBD plans), so this is a pure documentation
reorder/renumber with no execution history to reconcile and no content authored beyond
the swap itself.

Output: `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, and `.planning/STATE.md`
all internally consistent with the new order: Phase 1 = Concerns Remediation (no
dependency), Phase 2 = Foundations, Process & Vision (depends on Phase 1), Phases 3-5
unchanged in number/content except Phase 3's dependency line.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap Phase 1 and Phase 2 in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Make five scoped edits to `.planning/ROADMAP.md` (use the Edit tool for each, never a
    full-file rewrite). Do not touch Phase 3, 4, or 5 content beyond edit 4 below, and do
    not change any Requirements line's requirement IDs or any Success Criteria item text
    anywhere: only phase numbers, headings, and Depends-on values move.

    1. Overview paragraph: change the clause "It follows the natural dependency order
    surfaced in `.planning/codebase/CONCERNS.md` and the existing `TODO.md` milestone
    breakdown: first fix the *process* gap that would otherwise stall concept review
    (Phase 1), clear the flagged architecture/tech-debt blockers (Phase 2), do the bulk
    of language-concept design and anti-pattern research and run it through the review
    gate (Phase 3), settle the smaller cross-cutting Interaction Matrix question (Phase
    4), and finally settle the "Orthon" naming question and freeze the spec (Phase 5)."
    to "It follows the dependency order surfaced in `.planning/codebase/CONCERNS.md` and
    the existing `TODO.md` milestone breakdown: first clear the flagged
    architecture/tech-debt blockers (Phase 1), then fix the *process* gap that would
    otherwise stall concept review (Phase 2), do the bulk of language-concept design and
    anti-pattern research and run it through the review gate (Phase 3), settle the
    smaller cross-cutting Interaction Matrix question (Phase 4), and finally settle the
    "Orthon" naming question and freeze the spec (Phase 5)." Leave the following sentence
    ("Every phase in this roadmap is documentation/design work...") unchanged.

    2. `## Phases` summary list: swap the order and content of the two bullets so the
    list reads "- [ ] **Phase 1: Concerns Remediation** - Fill the four empty
    architecture specs and resolve the tech-debt items flagged in CONCERNS.md" followed
    by "- [ ] **Phase 2: Foundations, Process & Vision** - Define the concept acceptance
    gate, review process, and requirement-tracking model; review the Why-layer documents
    (Manifesto, Vision, Zen)". The Phase 3/4/5 bullets below stay exactly where they are.

    3. Phase Details section: relocate and renumber the two full section bodies (each
    runs from its `### Phase N:` heading down through its trailing `**Plans**: TBD`
    line) so the section currently headed `### Phase 2: Concerns Remediation` becomes
    the new `### Phase 1: Concerns Remediation` and appears FIRST (physically before
    the other), and the section currently headed `### Phase 1: Foundations, Process &
    Vision` becomes the new `### Phase 2: Foundations, Process & Vision` and appears
    SECOND. In each relocated section, change only two things: the heading number, and
    the `**Depends on**` line. The new Phase 1 (Concerns Remediation) section's
    Depends-on line becomes "**Depends on**: Nothing (first phase)"; the new Phase 2
    (Foundations, Process & Vision) section's Depends-on line becomes "**Depends on**:
    Phase 1". Every other line in both sections (Goal, Requirements, all Success
    Criteria items, Plans) must remain byte-for-byte identical to its current text: only
    the two sections' physical order, heading numbers, and Depends-on values change. The
    `### Phase 3:` section must immediately follow the relocated Phase 2 section, exactly
    as it follows Phase 2 today.

    4. `### Phase 3: Language Inventory, Anti-Pattern Research & Concept Design Review`
    section: change the Depends-on line from "**Depends on**: Phase 1 (requires the
    acceptance gate and review process from PROC-01/02)" to "**Depends on**: Phase 2
    (requires the acceptance gate and review process from PROC-01/02)" since PROC-01/02
    now live in the renumbered Phase 2. Do not change Phase 3's Requirements line, Goal,
    or Success Criteria.

    5. `## Progress` table: swap the order and content of the first two rows so the
    table reads "| 1. Concerns Remediation | 0/TBD | Not started | - |" followed by "| 2.
    Foundations, Process & Vision | 0/TBD | Not started | - |". The "Execution Order"
    line above the table ("Phases execute in numeric order: 1 -> 2 -> 3 -> 4 -> 5", using
    the file's arrow character) stays unchanged: numeric order is still 1-5, only the
    content of 1 and 2 swapped. Rows for Phases 3, 4, 5 stay exactly where they are.
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/orthon-lang/docs && bash -c 'set -e
grep -Fq "### Phase 1: Concerns Remediation" .planning/ROADMAP.md
grep -Fq "### Phase 2: Foundations, Process & Vision" .planning/ROADMAP.md
P1_LINE=$(grep -Fn "### Phase 1: Concerns Remediation" .planning/ROADMAP.md | head -1 | cut -d: -f1)
P2_LINE=$(grep -Fn "### Phase 2: Foundations, Process & Vision" .planning/ROADMAP.md | head -1 | cut -d: -f1)
[ "$P1_LINE" -lt "$P2_LINE" ]
sed -n "${P1_LINE},$((P1_LINE+5))p" .planning/ROADMAP.md | grep -Fq "**Depends on**: Nothing (first phase)"
sed -n "${P1_LINE},$((P1_LINE+5))p" .planning/ROADMAP.md | grep -Fq "DEBT-01"
sed -n "${P2_LINE},$((P2_LINE+5))p" .planning/ROADMAP.md | grep -Fq "**Depends on**: Phase 1"
sed -n "${P2_LINE},$((P2_LINE+5))p" .planning/ROADMAP.md | grep -Fq "PROC-01"
grep -Fq "**Depends on**: Phase 2 (requires the acceptance gate and review process from PROC-01/02)" .planning/ROADMAP.md
! grep -Fq "**Depends on**: Phase 1 (requires the acceptance gate and review process from PROC-01/02)" .planning/ROADMAP.md
grep -Fq "| 1. Concerns Remediation | 0/TBD | Not started | - |" .planning/ROADMAP.md
grep -Fq "| 2. Foundations, Process & Vision | 0/TBD | Not started | - |" .planning/ROADMAP.md
grep -Fq "**Phase 1: Concerns Remediation**" .planning/ROADMAP.md
grep -Fq "**Phase 2: Foundations, Process & Vision**" .planning/ROADMAP.md
grep -Fq "first clear the flagged architecture/tech-debt blockers (Phase 1)" .planning/ROADMAP.md
grep -Fq "then fix the *process* gap that would otherwise stall concept review (Phase 2)" .planning/ROADMAP.md
echo OK'</automated>
  </verify>
  <done>
    ROADMAP.md's Phase Details section lists "### Phase 1: Concerns Remediation" (Depends
    on: Nothing) before "### Phase 2: Foundations, Process & Vision" (Depends on: Phase
    1). Phase 3's Depends-on line references Phase 2. The Overview paragraph, Phases
    summary list, and Progress table all reflect the new order. Phases 3, 4, 5 content
    (other than Phase 3's Depends-on line) is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Propagate the renumbering into REQUIREMENTS.md and STATE.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
    Make two scoped edits to `.planning/REQUIREMENTS.md` and three scoped edits to
    `.planning/STATE.md` (use the Edit tool for each, never a full-file rewrite). Do not
    touch requirement IDs, the "### Milestone N" category headings, the Coverage totals,
    or the "Quick Tasks Completed" table / Session Continuity history in STATE.md.

    REQUIREMENTS.md:

    1. Traceability table: this table has a contiguous 24-row block covering PROC-01
    through VISION-03 (7 rows, currently "Phase 1") immediately followed by DEBT-01
    through DEBT-17 (17 rows, currently "Phase 2"). Flip the Phase column on every one of
    these 24 rows: PROC-01, PROC-02, PROC-03, PROC-04, VISION-01, VISION-02, and
    VISION-03 rows change from "Phase 1" to "Phase 2"; DEBT-01 through DEBT-17 rows
    change from "Phase 2" to "Phase 1". Row order and the "Pending" status column are
    unchanged. Rows for CONCEPT-*, ANTIPAT-*, XCUT-01, NAME-01, and FREEZE-* are
    untouched.

    2. `**Phase summary:**` block (near the end, below Coverage): swap the order and
    content of the first two bullets so it reads, using the file's existing em-dash
    style, a line for "Phase 1" naming Concerns Remediation with 17 requirements
    (DEBT-01..17), followed by a line for "Phase 2" naming Foundations, Process & Vision
    with 7 requirements (PROC-01..04, VISION-01..03) — i.e. exactly swap the phase
    number on the two existing bullet lines and swap their order, keeping each bullet's
    own title/count/requirement-list text unchanged. Leave the Phase 3/4/5 summary
    bullets exactly as they are.

    STATE.md:

    1. Change the "**Current focus:**" line so it reads Phase 1 followed by "Concerns
    Remediation" (keep the file's existing em-dash separator style, matching how the
    line currently separates "Phase 1" from the phase title).

    2. Change "Phase: 1 of 5 (Foundations, Process & Vision)" to "Phase: 1 of 5 (Concerns
    Remediation)".

    3. In the Accumulated Context section, make these prose edits so cited phase numbers
    match the new arrangement:
       - Decisions bullet: change "Roadmap: CONCERNS.md blockers/tech-debt regrouped
         into a dedicated Phase 2 (Concerns Remediation), sequenced after Phase 1
         (Foundations/Process) and before the main concept-design work in Phase 3, since
         the acceptance-gate process (Phase 1) and architecture-spec fill-in (Phase 2)
         both unblock later phases without depending on each other." to a rewritten
         bullet (keep the file's existing em-dash style for the parenthetical breaks):
         "Roadmap: CONCERNS.md blockers/tech-debt regrouped into a dedicated Concerns
         Remediation phase; per the 2026-07-20 swap decision, resequenced as Phase 1,
         run before Phase 2 (Foundations/Process), so tech-debt leftovers are cleared
         before investing in process/vision work; Phase 2 now depends on Phase 1
         completing first, and both still precede the main concept-design work in Phase
         3." Leave the other two Decisions bullets unchanged.
       - Blockers/Concerns bullet: change "Phase 1 must complete before Phase 3 can run
         the Concept Design Review gate (ANTIPAT-11) — PROC-01/02 define the acceptance
         criteria that review depends on." to "Phase 2 must complete before Phase 3 can
         run the Concept Design Review gate (ANTIPAT-11) — PROC-01/02 define the
         acceptance criteria that review depends on."
       - Blockers/Concerns bullet: change "Phase 2's DEBT-01..04 (IR/Parser/TypeSystem/
         NameResolution specs) must land before Phase 5 (Freeze) validates architectural
         completeness — see CONCERNS.md "Empty Placeholder Files" (Critical severity)."
         to "Phase 1 DEBT-01..04 (IR/Parser/TypeSystem/NameResolution specs) must land
         before Phase 5 (Freeze) validates architectural completeness — see
         CONCERNS.md "Empty Placeholder Files" (Critical severity)." Leave the third
         Blockers/Concerns bullet (about 18 of 22 concept documents / Phase 3) unchanged.

    Do not edit the "Quick Tasks Completed" table, "Deferred Items" table, or "Session
    Continuity" section: these are historical records, not forward-looking phase
    references, and the "Ready to run `/gsd-plan-phase 1`" line stays as-is since it
    names a phase number, not the old phase title.
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/orthon-lang/docs && bash -c 'set -e
grep -Fq "| PROC-01 | Phase 2 | Pending |" .planning/REQUIREMENTS.md
grep -Fq "| VISION-03 | Phase 2 | Pending |" .planning/REQUIREMENTS.md
grep -Fq "| DEBT-01 | Phase 1 | Pending |" .planning/REQUIREMENTS.md
grep -Fq "| DEBT-17 | Phase 1 | Pending |" .planning/REQUIREMENTS.md
grep -Fq "| CONCEPT-01 | Phase 3 | Pending |" .planning/REQUIREMENTS.md
grep -Fq "Concerns Remediation: 17 requirements (DEBT-01..17)" .planning/REQUIREMENTS.md
grep -Fq "Foundations, Process & Vision: 7 requirements (PROC-01..04, VISION-01..03)" .planning/REQUIREMENTS.md
PROC_LINE=$(grep -Fn "Foundations, Process & Vision: 7 requirements" .planning/REQUIREMENTS.md | head -1 | cut -d: -f1)
DEBT_LINE=$(grep -Fn "Concerns Remediation: 17 requirements" .planning/REQUIREMENTS.md | head -1 | cut -d: -f1)
[ "$DEBT_LINE" -lt "$PROC_LINE" ]
sed -n "${DEBT_LINE}p" .planning/REQUIREMENTS.md | grep -Fq "Phase 1"
sed -n "${PROC_LINE}p" .planning/REQUIREMENTS.md | grep -Fq "Phase 2"
grep -Fq "Mapped to phases: 57 (100%)" .planning/REQUIREMENTS.md
grep -Fq "Current focus:** Phase 1" .planning/STATE.md
CF_LINE=$(grep -Fn "Current focus:** Phase 1" .planning/STATE.md | head -1 | cut -d: -f1)
sed -n "${CF_LINE}p" .planning/STATE.md | grep -Fq "Concerns Remediation"
grep -Fq "Phase: 1 of 5 (Concerns Remediation)" .planning/STATE.md
grep -Fq "Phase 2 must complete before Phase 3 can run the Concept Design Review gate" .planning/STATE.md
grep -Fq "Phase 1 DEBT-01..04 (IR/Parser/TypeSystem/NameResolution specs) must land before Phase 5" .planning/STATE.md
grep -Fq "resequenced as Phase 1" .planning/STATE.md
grep -Fq "260720-e24" .planning/STATE.md
echo OK'</automated>
  </verify>
  <done>
    REQUIREMENTS.md traceability table maps DEBT-01..17 to Phase 1 and PROC-01..04 /
    VISION-01..03 to Phase 2; the Phase summary block lists Concerns Remediation (Phase
    1) before Foundations, Process & Vision (Phase 2), and Coverage stays 57/57 (100%).
    STATE.md's Current focus, Current Position, one Decisions bullet, and two
    Blockers/Concerns bullets all cite the corrected phase numbers. Quick Tasks Completed
    and Session Continuity history are untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan edits three static Markdown planning documents
(`.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md`) in a
documentation-only repository — no code, no user input, no external service, no
execution surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick260720el7-01 | Tampering | .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md | low | accept | Plain-text Markdown edits reviewed via automated grep verification in each task, including cross-file phase-mapping consistency checks; changes are committed to git for auditability. |
</threat_model>

<verification>
1. `.planning/ROADMAP.md`: Phase 1 (heading "Concerns Remediation") physically precedes
   Phase 2 (heading "Foundations, Process & Vision") in the Phase Details section; Phase
   1 depends on nothing, Phase 2 depends on Phase 1; Phase 3 depends on Phase 2; the
   Overview paragraph, `## Phases` summary list, and `## Progress` table all reflect the
   new order; Phases 3, 4, 5 content is otherwise untouched.
2. `.planning/REQUIREMENTS.md`: Traceability table maps DEBT-01..17 to Phase 1 and
   PROC-01..04 / VISION-01..03 to Phase 2; Phase summary block lists Concerns Remediation
   before Foundations, Process & Vision; Coverage totals stay 57/57 (100%) unmapped: 0.
3. `.planning/STATE.md`: Current focus and Current Position both name Phase 1 as
   Concerns Remediation; the Decisions bullet describing the Phase 1/2 sequencing and the
   two Blockers/Concerns bullets citing PROC-01/02 and DEBT-01..04 all cite the corrected
   phase numbers; Quick Tasks Completed table and Session Continuity history unchanged.
4. Cross-file consistency: `grep -n "DEBT-01" .planning/ROADMAP.md .planning/REQUIREMENTS.md`
   and `grep -n "PROC-01" .planning/ROADMAP.md .planning/REQUIREMENTS.md` show DEBT-01
   associated with Phase 1 and PROC-01 associated with Phase 2 in both files.
</verification>

<success_criteria>
- ROADMAP.md's Phase 1 is "Concerns Remediation" (depends on nothing) and Phase 2 is
  "Foundations, Process & Vision" (depends on Phase 1), in that physical file order.
- ROADMAP.md's Phase 3 dependency line references Phase 2, not Phase 1.
- Phases 3, 4, and 5 in ROADMAP.md keep their numbers, requirements, and success
  criteria unchanged (other than Phase 3's dependency line).
- REQUIREMENTS.md's traceability table and Phase summary block agree with ROADMAP.md's
  new phase mapping; Coverage totals (57/57, 100%) are unaffected by the reordering.
- STATE.md's Current focus/Current Position and the phase-number-citing prose bullets in
  Decisions and Blockers/Concerns are internally consistent with the new order; no
  historical record (Quick Tasks Completed, Session Continuity) is altered.
- No new content is authored beyond the swap: Goal text, Success Criteria items, and
  requirement ID lists for every phase are unchanged from their current wording.
</success_criteria>

<output>
Create `.planning/quick/260720-el7-swap-phase-1-and-phase-2-in-roadmap-md-c/260720-el7-SUMMARY.md` when done
</output>
