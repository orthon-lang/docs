---
phase: quick-260726-tz1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [how/EXPLORING_THE_DOCS.md, README.md, AGENTS.md]
autonomous: true

must_haves:
  truths:
    - "how/EXPLORING_THE_DOCS.md exists, has exactly one Level 1 heading, and explains what graphify is, why it helps on this ~91-file repo, how to invoke it (/graphify as a Claude Code skill, plus a note for non-Claude-Code agents), and gives 5 concrete example queries tied to this repo's real structure"
    - "Every markdown cross-reference link added in how/EXPLORING_THE_DOCS.md resolves to a file that actually exists in the repository, using paths relative to how/ per AGENTS.md rule 8"
    - "README.md's existing Getting Started table gains exactly one new row pointing at how/EXPLORING_THE_DOCS.md, matching the table's existing terse two-column style; no other README.md content changes"
    - "AGENTS.md's §3 Document Map table gains exactly one new row for how/EXPLORING_THE_DOCS.md (Meta layer); no other AGENTS.md content changes"
    - "All new text in all three files is English only"
    - "what/GLOSSARY.md is not modified (graphify is a tool name, not a new Orthon language term)"
  artifacts:
    - how/EXPLORING_THE_DOCS.md
  key_links:
    - "README.md Getting Started table -> how/EXPLORING_THE_DOCS.md"
    - "AGENTS.md §3 Document Map -> how/EXPLORING_THE_DOCS.md"
    - "how/EXPLORING_THE_DOCS.md -> why/VISION.md, what/CORE_CONCEPTS.md, how/DESIGN_PRINCIPLES.md, how/decision_records/INDEX.md, how/concepts/research/essential/EXECUTION_PROGRAM.md, README.md"
---

<objective>
Add a short How-layer guide, `how/EXPLORING_THE_DOCS.md`, that tells a new contributor
or AI agent how to use the `graphify` Claude Code skill to build a knowledge graph over
this repository's ~91 Markdown files and query it — then link that guide from README.md's
Getting Started table and AGENTS.md's Document Map so it's discoverable from both existing
entry points.

Purpose: The repo already has a lot of working documentation but no pointer telling a
newcomer how to explore its internal structure (the Why/How/What/When layers, the EDR
index, the heavy cross-referencing between concept research files) beyond reading files
one at a time. `graphify` is already available in this environment and is a good fit for
that exploration task.

Output: One new file (`how/EXPLORING_THE_DOCS.md`) plus one additive row each in
`README.md` and `AGENTS.md`. No other file changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@README.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create how/EXPLORING_THE_DOCS.md</name>
  <files>how/EXPLORING_THE_DOCS.md</files>
  <action>
    Create a new file at `how/EXPLORING_THE_DOCS.md` (How layer, top-level — same tier as
    `how/DESIGN_PRINCIPLES.md` and `how/CONCEPT_PIPELINE.md`: a process/tooling guide, not
    language-spec content, so it does not belong under `what/`). Single Level 1 heading at
    the top: "Exploring the Docs with graphify". Structure the body with these Level 2
    sections, in this order:

    1. **What graphify is** — one short paragraph: a Claude Code skill that turns any
       input (code, docs, papers) into a persistent knowledge graph with god nodes,
       community detection, and query/path/explain tools.

    2. **Why it helps on this repo** — one short paragraph naming the specific pain point:
       ~91 Markdown files organized into the Why/How/What/When (Golden Circle) layers,
       heavy explicit cross-referencing between them (per AGENTS.md's linking rules), a
       separate EDR index (`how/decision_records/INDEX.md`), and a unified glossary
       (`what/GLOSSARY.md`) — a graph view makes the actual link topology (and any
       orphaned or under-linked documents) visible in a way that reading files
       sequentially does not.

    3. **How to invoke it** — state plainly that `graphify` is already available as a
       Claude Code skill in this environment: invoke it with `/graphify` in a Claude Code
       session, no separate installation needed. Add one short note for readers not
       running Claude Code: they would need the equivalent skill/tool installed in their
       own agent tooling first.

    4. **Example queries for this repository** — exactly 5 examples, each as a Level 3
       heading "### Query 1:" through "### Query 5:" followed by one or two sentences of
       query text tailored to this repo's real structure (name real paths, not
       placeholders):
       - Query 1: trace how the five pillars in `why/VISION.md` are referenced across
         `how/DESIGN_PRINCIPLES.md` and the concept research tree under
         `how/concepts/research/`; note that `what/CORE_CONCEPTS.md` (the intended
         accepted-concept registry) is currently empty, so this traversal will land in
         research drafts rather than accepted concepts until Phase 4 populates it.
       - Query 2: find which EDRs under `how/decision_records/` link to
         `how/DESIGN_PRINCIPLES.md`, to see which design principles already have a
         recorded decision behind them and which don't yet.
       - Query 3: find every document that references the Execution Program / Execution
         Engine concept, rooted at `how/concepts/research/essential/EXECUTION_PROGRAM.md`,
         cross-checked against its description in `why/VISION.md`.
       - Query 4: find orphaned documents — files with zero incoming cross-references — as
         a fast way to spot dead-end research drafts or files missing from AGENTS.md's
         Document Map.
       - Query 5: render the full document graph rooted at `README.md`'s Getting Started
         table, to see how far each entry point's link-reach actually extends into `how/`
         and `what/`.

    5. **A short closing note** stating that graphify's graph is an exploration aid, not a
       source of truth — accepted design decisions still live only in EDRs
       (`how/decision_records/`) and accepted concepts (`what/CORE_CONCEPTS.md`).

    Use markdown links (not bare filenames) for every cross-referenced path, relative to
    `how/` per AGENTS.md rule 8 (e.g. `../why/VISION.md`, `../what/CORE_CONCEPTS.md`,
    `DESIGN_PRINCIPLES.md`, `decision_records/INDEX.md`,
    `concepts/research/essential/EXECUTION_PROGRAM.md`, `../README.md`). Write the entire
    file in English. Do not add or restate any language-design decision — this is a
    tooling/process note, not a concept document, so it needs no EDR and no GLOSSARY.md
    entry.
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/orthon-lang/docs && test -f how/EXPLORING_THE_DOCS.md && test -f why/VISION.md && test -f what/CORE_CONCEPTS.md && test -f how/DESIGN_PRINCIPLES.md && test -f how/decision_records/INDEX.md && test -f how/concepts/research/essential/EXECUTION_PROGRAM.md && [ "$(grep -c '^# ' how/EXPLORING_THE_DOCS.md)" -eq 1 ] && [ "$(grep -c '^### Query' how/EXPLORING_THE_DOCS.md)" -eq 5 ] && grep -q '/graphify' how/EXPLORING_THE_DOCS.md && grep -q '(../why/VISION.md)' how/EXPLORING_THE_DOCS.md && grep -q '(../what/CORE_CONCEPTS.md)' how/EXPLORING_THE_DOCS.md && grep -q '(DESIGN_PRINCIPLES.md)' how/EXPLORING_THE_DOCS.md && grep -q '(decision_records/INDEX.md)' how/EXPLORING_THE_DOCS.md && grep -q '(concepts/research/essential/EXECUTION_PROGRAM.md)' how/EXPLORING_THE_DOCS.md && grep -q '(../README.md)' how/EXPLORING_THE_DOCS.md && echo OK</automated>
  </verify>
  <done>
    `how/EXPLORING_THE_DOCS.md` exists with a single H1, a What/Why/How-to-invoke/
    Example-queries/closing-note structure, exactly 5 "### Query N:" headings naming real
    repo paths, and every cross-reference resolves to an existing file using a path
    relative to `how/`. All text is English.
  </done>
</task>

<task type="auto">
  <name>Task 2: Link the guide from README.md's Getting Started table</name>
  <files>README.md</files>
  <action>
    Add exactly one new row to the end of README.md's existing "## Getting Started" table
    (the two-column `| Document | What it tells you |` table currently ending with the
    Decision Records row), in the same terse style as the existing rows:
    `| [Exploring the Docs](how/EXPLORING_THE_DOCS.md) | How to use graphify to build and
    query a knowledge graph over this repo's docs |`. Do not reword, reorder, or otherwise
    touch any of the table's 8 existing rows, and do not add any other paragraph or
    section to README.md — this is a single additive table row.
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/orthon-lang/docs && [ "$(grep -c '^| \[' README.md)" -eq 9 ] && grep -q '\[Exploring the Docs\](how/EXPLORING_THE_DOCS.md)' README.md && grep -q '\[Manifesto\](why/MANIFESTO.md)' README.md && grep -q '\[Decision Records\](how/decision_records/INDEX.md)' README.md && echo OK</automated>
  </verify>
  <done>
    README.md's Getting Started table has 9 data rows (8 original + 1 new), the new row
    links to `how/EXPLORING_THE_DOCS.md`, and all 8 original rows are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add the guide to AGENTS.md's Document Map</name>
  <files>AGENTS.md</files>
  <action>
    In AGENTS.md §3 "Document Map" table, insert one new row for the new guide,
    positioned directly after the existing `` `AGENTS.md` | Meta | ... `` row (grouping it
    with the other Meta-layer reference docs such as `` `what/GLOSSARY.md` ``), matching
    the table's existing convention of wrapping the filename cell in backticks:
    `` | `how/EXPLORING_THE_DOCS.md` | Meta | Guide to exploring the doc set's
    cross-reference graph using the graphify tool | ``. Do not modify any other row, and
    do not touch the §4.3 File Structure ASCII tree or any other section of AGENTS.md —
    this is a single additive table row in §3 only.
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/orthon-lang/docs && grep -q '| `how/EXPLORING_THE_DOCS.md` | Meta |' AGENTS.md && grep -q '| `AGENTS.md` | Meta |' AGENTS.md && grep -q '| `what/GLOSSARY.md` | Meta |' AGENTS.md && echo OK</automated>
  </verify>
  <done>
    AGENTS.md §3 Document Map has one new row for `how/EXPLORING_THE_DOCS.md` (Meta
    layer), positioned near the other Meta-layer rows; no other row or section changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan adds one new static Markdown guide and two additive table rows in a
documentation-only repository — no code, no user input, no external service, no
execution surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick260726tz1-01 | Tampering | how/EXPLORING_THE_DOCS.md cross-reference links | low | mitigate | Task 1's automated verify checks that every referenced target file exists on disk (`test -f`) and that the exact expected link syntax is present, catching a dangling or mistyped cross-reference before commit. |
| T-quick260726tz1-02 | Tampering | README.md / AGENTS.md existing content | low | mitigate | Task 2 and Task 3 verifies assert both an original row (Manifesto / AGENTS.md itself) and the new row are present, catching accidental loss of existing content from a careless whole-section edit. |
| T-quick260726tz1-03 | Information Disclosure | how/EXPLORING_THE_DOCS.md content | low | accept | The guide only describes a publicly available Claude Code skill and repo-internal file paths already public in this open-source spec repo; no secrets or credentials are involved. |

</threat_model>

<verification>
1. `how/EXPLORING_THE_DOCS.md` exists with one H1, the required sections, exactly 5
   "### Query N:" headings naming real repo paths, and every cross-reference resolves to
   an existing file.
2. README.md's Getting Started table has 9 data rows; the new row links to
   `how/EXPLORING_THE_DOCS.md`; all 8 original rows are unchanged.
3. AGENTS.md §3 Document Map has a new `how/EXPLORING_THE_DOCS.md` (Meta) row; no other
   row or section changed.
4. `what/GLOSSARY.md` is untouched.
5. All new text in all three files is English only (manual scan per AGENTS.md §10.9).
</verification>

<success_criteria>
- A newcomer or AI agent can find `how/EXPLORING_THE_DOCS.md` from either README.md's
  Getting Started table or AGENTS.md's Document Map.
- The guide explains what graphify is, why it's useful on this repo, how to invoke it
  (`/graphify`), and gives 5 example queries tied to this repo's actual file structure.
- Every cross-reference link added anywhere in this plan resolves to a real file.
- No file outside `how/EXPLORING_THE_DOCS.md`, `README.md`, and `AGENTS.md` is touched;
  `what/GLOSSARY.md` and `how/decision_records/` are untouched.
</success_criteria>

<output>
Create `.planning/quick/260726-tz1-add-docs-guide-for-using-graphify-to-exp/260726-tz1-SUMMARY.md` when done
</output>