---
phase: quick-260726-s31
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/REQUIREMENTS.md, .planning/seeds/SEED-001-concept-research-tier-triage.md, how/concepts/research/README.md]
autonomous: true
requirements: [PROC-03]

must_haves:
  truths:
    - "SEM-01..03 (Phase 2) name the 10 essential-tier source files that feed the Semantic Model, matching the classification in .planning/notes/2026-07-26-tier-vs-phase-mapping.md"
    - "PRIM-01..03 (Phase 3) name the 10 essential-tier source files that feed Primitive Blocks, matching the same classification note"
    - "CONCEPT-01..13 (13 hardcoded concept names from the old ~22-file inventory) no longer exist in REQUIREMENTS.md; replaced by 4 tier-scaled requirements (CONCEPT-ESS-01, CONCEPT-IMP-01, CONCEPT-DEFER-01, CONCEPT-REJECT-01) sequenced essential-first per SEED-001"
    - "The 3 Policy-pocket essential files (ALLOCATION.md, REGION_BASED_MEMORY_MANAGEMENT.md, EXECUTION_PROGRAM.md) are still mapped to a requirement (CONCEPT-ESS-01) with a footnote pointing at the pending relocation todo — not silently dropped"
    - "The 2 files a concurrent task (260726-s6t, commit bf0f10a) already moved from important/ to essential/ during this planning session (CONTEXT_PARAMETERS.md, REPRESENTATION_MODIFIERS.md) are flagged in CONCEPT-ESS-01 as pending their own Phase 2/3 classification, not silently folded into the already-decided 20-file list or left undiscoverable; DECLARATION_BY_ASSIGNMENT.md remains flagged as a footnote on CONCEPT-IMP-01 (arguably Phase 5 material)"
    - "The Traceability table's row count, the Coverage block's total, and the Phase 4 line in the Phase summary are all mutually consistent with each other and with the actual IDs listed in the requirement sections above them"
    - "SEED-001 and how/concepts/research/README.md each carry a short caveat pointing at the Phase 2/3/4/Policy split, without their existing triage tables being rewritten"
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/seeds/SEED-001-concept-research-tier-triage.md
    - how/concepts/research/README.md
  key_links:
    - "REQUIREMENTS.md Phase 2/Phase 3/Phase 4 sections <-> .planning/notes/2026-07-26-tier-vs-phase-mapping.md classification lists"
    - "REQUIREMENTS.md Traceability table rows <-> the exact requirement IDs used in the Phase 2/3/4 section bodies above it"
    - "REQUIREMENTS.md Coverage total <-> the actual row count of the Traceability table (verify by counting, not by trusting the previous stated figure — see Task 2 note on a pre-existing miscount)"
---

<objective>
Rewrite the Phase 2 (SEM-01..03), Phase 3 (PRIM-01..03), and Phase 4 (CONCEPT-01..13 and
surrounding counts) sections of `.planning/REQUIREMENTS.md` so they match the real
132-file `how/concepts/research/` inventory instead of the ~22-file inventory they were
scoped against on 2026-07-20. Add matching caveat notes to `SEED-001` and
`how/concepts/research/README.md` so neither document contradicts the new split.

**Live-inventory note:** counts below were verified at plan-write time
(2026-07-26, after commit bf0f10a). `how/concepts/research/` tiers are in constant
motion during Phase 4 prep — CONCEPT-IMP-01 and CONCEPT-DEFER-01 are deliberately
worded to reference tier directories + a snapshot count rather than hardcode every
filename, precisely so a future tier move like this doesn't re-stale this file. If
the executor finds different counts when applying this plan (re-run: `for d in
essential important deferrable reject; do echo "$d:
$(ls how/concepts/research/$d 2>/dev/null | grep -c '\''\.md$'\'')"; done`), use the
counts on disk at execution time for the two file-count mentions in CONCEPT-IMP-01
and CONCEPT-DEFER-01's action text and prose, while keeping the four requirement
IDs, the enumerated CONCEPT-ESS-01/CONCEPT-REJECT-01 file lists, and every
Traceability/Coverage row count exactly as specified (those do not depend on tier
population, only on which of the 4 replacement IDs exist).

This implements the classification already decided in
`.planning/notes/2026-07-26-tier-vs-phase-mapping.md`, reconciled against the
current tier snapshot (essential 42 / important 36 / deferrable 54 / reject 0 —
132 total, unchanged from the note's 40/38/54/0 split; only the essential/important
boundary shifted by 2 files mid-session):
- 10 of the 42 essential files are Semantic Model raw material -> feed Phase 2 (per
  the tier-vs-phase-mapping note; unaffected by the mid-session move below).
- 10 of the 42 essential files are Primitive Block raw material -> feed Phase 3
  (same — unaffected).
- 20 of the 42 essential files (17 feature-level + 3 Policy-pocket) were already a
  finalized Phase 4 classification before this session started.
- The remaining 2 essential files (`CONTEXT_PARAMETERS.md`, `REPRESENTATION_MODIFIERS.md`)
  arrived in `essential/` via a concurrent task (260726-s6t, commit `bf0f10a`,
  "move CONTEXT_PARAMETERS.md and REPRESENTATION_MODIFIERS.md to essential tier")
  that landed mid-session, AFTER `.planning/notes/2026-07-26-tier-vs-phase-mapping.md`
  was written and classified only the original 40 essential files. That commit is a
  pure tier relocation (important -> essential) with no stated Phase 2/3/4
  assignment. Do NOT guess which of Phase 2/3/4 they belong to here — flag them,
  same conservative treatment as the Policy pocket, inside CONCEPT-ESS-01 (making
  its total 22, not 20), pending a follow-up classification decision.
- Total Phase 4 essential-leftover is therefore 22 (17 + 3 + 2), not 20.
- The 3 Policy-pocket files (ALLOCATION, REGION_BASED_MEMORY_MANAGEMENT,
  EXECUTION_PROGRAM) keep their current Phase-4 status untouched in this task — a
  separate todo (`move-policy-level-essential-concepts-out-of-pipeline.md`) owns
  relocating them later. Do not reclassify them here.
- `important/` (now 36 files, was 38 before the mid-session move) and `deferrable/`
  (54 files) are NOT redistributed into Phase 2/3 — both tiers stay entirely Phase 4
  material, per the tier-vs-phase-mapping note. One borderline important-tier file
  remains after the mid-session move: `DECLARATION_BY_ASSIGNMENT.md` (arguably
  Phase 5 material) — gets a footnote flag only, not a move.
- Phase 4 total real input is unchanged in aggregate at 112 files (22 + 36 + 54):
  the mid-session tier move shifted 2 files between essential and important, but
  both tiers still land in Phase 4, so the aggregate total is unaffected.

Design choice for the Phase 4 replacement: CONCEPT-ESS-01 enumerates its 22 files
explicitly (20 finalized + 2 freshly-arrived-and-unclassified) because that list is
a small, name-worthy classification decision — worth the traceability. CONCEPT-IMP-01
and CONCEPT-DEFER-01 reference their tier directories and a file count instead of
enumerating every filename, because those two tiers are still actively changing
(the mid-session move is a live example) and enumerating them would reintroduce the
exact staleness problem this task is fixing. CONCEPT-REJECT-01 enumerates its 4
files because that list is also a finalized SEED-001 decision (the
principle-contradicting candidates).

While rewriting the Traceability/Coverage block, a pre-existing bug was found
independent of this task's scope: the table has always contained 79 rows (verified
by direct count), not the 66 the Coverage block and 2026-07-21 changelog claim.
This task corrects that number as part of updating the same block for the Phase 4
ID change (see Task 2) rather than leaving a second wrong total behind.

Purpose: Without this, `/gsd-plan-phase 4` either silently under-scopes Phase 4
against the stale 13-item CONCEPT list, or has to re-derive this file-to-phase
mapping from scratch. The classification work is already done; this task is
transcribing it into the requirements ledger that Phase 4 planning will read.

Output: Updated `.planning/REQUIREMENTS.md` with accurate Phase 2/3/4 sections and
an internally consistent Traceability table/Coverage block/Phase summary; caveat
notes added to `SEED-001` and `how/concepts/research/README.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/notes/2026-07-26-tier-vs-phase-mapping.md
@.planning/todos/pending/rewrite-phase-2-3-4-requirements-for-concept-inventory.md
@.planning/todos/pending/move-policy-level-essential-concepts-out-of-pipeline.md
@.planning/seeds/SEED-001-concept-research-tier-triage.md
@how/concepts/research/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Phase 2 (SEM-01..03) and Phase 3 (PRIM-01..03) sections to name their source files</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    In `.planning/REQUIREMENTS.md`, find the section starting at the heading
    "### Semantic Model — Phase 2" and ending right before "### Primitive Blocks —
    Phase 3" (currently the SEM-01, SEM-02, SEM-03 bullet lines). Replace the three
    bullet lines with:

    - **SEM-01**: `what/SEMANTIC_MODEL.md` synthesizes the 10 essential-tier source
      files identified as Semantic-Model raw material (`how/concepts/research/essential/DATA_MODEL.md`,
      `OWNERSHIP.md`, `OWNERSHIP_METAPROPERTY.md`, `OWNERSHIP_TRANSFER_OPERATOR.md`,
      `MUTABILITY.md`, `VALUE_SEMANTICS.md`, `IDENTITY_BASED_SAFETY.md`,
      `VISIBILITY_AND_ENCAPSULATION.md`, `SCOPED_RESOURCE_LIFECYCLE.md`,
      `EXPRESSION_ORIENTED_LANGUAGE.md`) into all 6 semantic dimensions (Identity,
      Ownership, Mutation, Evaluation, Visibility, Lifetime) as one coherent whole,
      replacing the current DRAFT placeholder
    - **SEM-02**: Cross-dimension conflicts across the 10 source files checked and
      resolved; validated against every Design Principle
    - **SEM-03**: EDR accepting the Semantic Model, recording how each of the 10
      source files' proposals were merged, modified, or superseded

    Keep the checkbox format (`- [ ] **SEM-01**: ...` etc.) matching the file's
    existing style — do not drop the `- [ ]` markers.

    Then find the section starting at "### Primitive Blocks — Phase 3" and ending
    right before "### Derived Features & Decision Pipeline — Phase 4" (the PRIM-01,
    PRIM-02, PRIM-03 lines). Replace them with:

    - **PRIM-01**: `what/PRIMITIVE_BLOCKS.md` synthesizes the 10 essential-tier
      source files identified as Primitive-Block raw material
      (`how/concepts/research/essential/FOUNDATIONAL_ABSTRACTIONS.md`,
      `EXCLUSIVE_DECLARATIONS.md`, `STRUCT_AS_VALUE_TYPE.md`, `CLASS_WITH_ACT.md`,
      `ACT_AS_FUNCTION.md`, `FUNCTIONS.md`, `FINAL_BY_DEFAULT.md`, `NAMESPACES.md`,
      `DELEGATE.md`, `COMPOSITION_OVER_INHERITANCE.md`) into the minimal orthogonal
      primitive set plus composition rules, replacing the current DRAFT placeholder
    - **PRIM-02**: Every concept research doc across all tiers
      (`how/concepts/research/{essential,important,deferrable,reject}/`, ~132 files)
      verified to decompose onto the primitive set; set confirmed minimal
    - **PRIM-03**: EDR accepting the Primitive Blocks set, recording how each of the
      10 source files' proposals were merged, modified, or superseded

    Same checkbox format. Do not touch any other section in this pass — Phase 4 is
    handled in Task 2. Note: neither of the two files a concurrent task moved into
    `essential/` mid-session (`CONTEXT_PARAMETERS.md`, `REPRESENTATION_MODIFIERS.md`)
    belongs in either 10-file list above — they are handled in Task 2's
    CONCEPT-ESS-01, not here. Do not add them to SEM-01 or PRIM-01's file lists.
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/orthon-lang/docs && test $(grep -oE "DATA_MODEL\.md|OWNERSHIP\.md|OWNERSHIP_METAPROPERTY\.md|OWNERSHIP_TRANSFER_OPERATOR\.md|MUTABILITY\.md|VALUE_SEMANTICS\.md|IDENTITY_BASED_SAFETY\.md|VISIBILITY_AND_ENCAPSULATION\.md|SCOPED_RESOURCE_LIFECYCLE\.md|EXPRESSION_ORIENTED_LANGUAGE\.md" .planning/REQUIREMENTS.md | sort -u | wc -l) -eq 10 && test $(grep -oE "FOUNDATIONAL_ABSTRACTIONS\.md|EXCLUSIVE_DECLARATIONS\.md|STRUCT_AS_VALUE_TYPE\.md|CLASS_WITH_ACT\.md|ACT_AS_FUNCTION\.md|FUNCTIONS\.md|FINAL_BY_DEFAULT\.md|NAMESPACES\.md|DELEGATE\.md|COMPOSITION_OVER_INHERITANCE\.md" .planning/REQUIREMENTS.md | sort -u | wc -l) -eq 10 && echo PASS</automated>
  </verify>
  <done>SEM-01..03 name all 10 Semantic-Model source files and PRIM-01..03 name all 10 Primitive-Block source files, each present at least once in REQUIREMENTS.md; no other section was modified in this task.</done>
</task>

<task type="auto">
  <name>Task 2: Replace CONCEPT-01..13 with tier-scaled requirements and reconcile Traceability/Coverage/Phase-summary counts</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Before editing, re-check the live tier counts (they may have moved again since
    this plan was written):
    `for d in essential important deferrable reject; do echo "$d:
    $(ls how/concepts/research/$d 2>/dev/null | grep -c '\.md$')"; done`.
    Use whatever the current `important` and `deferrable` counts are for the two
    file-count mentions below (this plan was written against 36 important / 54
    deferrable); the 4 requirement IDs, the CONCEPT-ESS-01/CONCEPT-REJECT-01
    enumerated file lists, and the Traceability row math do not change regardless.

    In `.planning/REQUIREMENTS.md`, find "### Derived Features & Decision Pipeline —
    Phase 4" through its DERIV-03 line (the whole Phase 4 requirement list). Replace
    the CONCEPT-01 through CONCEPT-13 bullet lines (13 lines) with these 4 lines,
    inserted in the same position (immediately after DERIV-01, immediately before
    ANTIPAT-01). Leave DERIV-01, ANTIPAT-01..10, DERIV-02, and DERIV-03 exactly as
    they are today — do not reword them:

    - **CONCEPT-ESS-01**: The 22 essential-tier concepts not consumed by Phase 2 or
      Phase 3 pass the Decision Pipeline first, per SEED-001's essential-first
      sequencing, each reaching EDR-accepted or explicitly-rejected status — 17 core
      concepts (`how/concepts/research/essential/AST_MACROS.md`,
      `COMPILER_AS_STATIC_ANALYZER.md`, `COMPILE_TIME_EXECUTION.md`,
      `COMPOSABLE_COLLECTION_OPS.md`, `CONCURRENCY_MODEL.md`, `EQUALITY.md`,
      `ERROR_HANDLING.md`, `ERROR_UNION.md`, `GENERICS.md`, `ITERATOR_PROTOCOL.md`,
      `LAZY_SEQUENCE_GENERATORS.md`, `NULL_SAFETY.md`, `PATTERN_MATCHING.md`,
      `PATTERN_MATCHING_DISPATCH.md`, `TRAITS.md`, `TYPE_INFERENCE.md`,
      `TYPE_LEVEL_NULL_SAFETY.md`), plus 3 Policy-level pocket concepts processed
      here pending relocation (`ALLOCATION.md`, `REGION_BASED_MEMORY_MANAGEMENT.md`,
      `EXECUTION_PROGRAM.md` — see
      `.planning/todos/pending/move-policy-level-essential-concepts-out-of-pipeline.md`),
      plus 2 concepts a concurrent task moved from `important/` into `essential/`
      mid-cycle without a Phase assignment (`CONTEXT_PARAMETERS.md`,
      `REPRESENTATION_MODIFIERS.md` — commit `bf0f10a`; still pending a Phase 2 vs.
      Phase 3 vs. Phase 4 call, tracked here as unclassified rather than guessed)
    - **CONCEPT-IMP-01**: All important-tier concepts
      (`how/concepts/research/important/`, 36 files as of 2026-07-26) pass the
      Decision Pipeline as a second pass, after CONCEPT-ESS-01 completes. One
      borderline file flagged for a future separate judgment call rather than
      moved: `DECLARATION_BY_ASSIGNMENT.md` is arguably Phase 5 (Syntax) material
    - **CONCEPT-DEFER-01**: All deferrable-tier concepts
      (`how/concepts/research/deferrable/`, 54 files as of 2026-07-26) explicitly
      deferred to v0.2/v0.3 with a documented one-paragraph rationale per concept,
      except the 4 files covered by CONCEPT-REJECT-01
    - **CONCEPT-REJECT-01**: The 4 concepts flagged in SEED-001 as contradicting
      Orthon's stated principles — `PROTOTYPE.md`, `SIGNIFICANT_WHITESPACE.md`,
      `DYNAMIC_TYPING.md`, `CLASS_OR_STRUCTURE_AS_PRIMARY_COMPOSITION.md` (currently
      in `how/concepts/research/deferrable/`) — evaluated for formal rejection via
      EDR; rejected files moved to `how/concepts/research/reject/`

    Same `- [ ] **ID**: ...` checkbox format as the rest of the file.

    Next, in the "## Traceability" table, find the 13 rows for `CONCEPT-01` through
    `CONCEPT-13` (each `| CONCEPT-NN | Phase 4 | Pending |`) and replace them with 4
    rows in the same position:
    `| CONCEPT-ESS-01 | Phase 4 | Pending |`,
    `| CONCEPT-IMP-01 | Phase 4 | Pending |`,
    `| CONCEPT-DEFER-01 | Phase 4 | Pending |`,
    `| CONCEPT-REJECT-01 | Phase 4 | Pending |`.

    Next, in the "**Coverage:**" block directly below the table, before making any
    edit, count the table's actual current row count with:
    `awk '/^## Traceability/,/^\*\*Coverage/' .planning/REQUIREMENTS.md | grep -cE '^\| [A-Z][A-Z0-9-]* \|'`
    (this returns 79 before this task's edits — a pre-existing miscount unrelated to
    the Phase 4 rewrite; the file currently states 66). After deleting 13 rows and
    adding 4 (net -9), the correct new total is 79 - 9 = 70. Replace the Coverage
    block's three lines with:
    "- v1 requirements: 70 total",
    "- Mapped to phases: 70 (100%)",
    "- Unmapped: 0".

    Next, in the "**Phase summary:**" bullet list below Coverage, replace the Phase
    4 line with:
    "- Phase 4 — Derived Features & Decision Pipeline: 17 requirements (DERIV-01..03,
    CONCEPT-ESS-01, CONCEPT-IMP-01, CONCEPT-DEFER-01, CONCEPT-REJECT-01,
    ANTIPAT-01..10)". Leave every other Phase summary bullet unchanged.

    Finally, append one new changelog line after the last existing
    "*Last updated: ...*" line at the bottom of the file:
    "*Last updated: 2026-07-26 — Phase 2 (SEM-01..03) and Phase 3 (PRIM-01..03) now
    name their actual essential-tier source files (10 each, per
    `.planning/notes/2026-07-26-tier-vs-phase-mapping.md`); Phase 4's thirteen
    individually-hardcoded per-concept requirements (scoped when
    `how/concepts/research/` held ~22 files) replaced with 4 tier-scaled
    requirements — CONCEPT-ESS-01 (22 files, including 2 files a concurrent task
    moved into essential/ mid-cycle pending Phase classification),
    CONCEPT-IMP-01 (36 files), CONCEPT-DEFER-01 (54 files), CONCEPT-REJECT-01 (4
    files) — covering the real 132-file inventory (42 essential / 36 important / 54
    deferrable / 0 reject; 20 of the 42 essential files consumed by Phase 2/3),
    sequenced essential-first per SEED-001. Also corrected a pre-existing
    Traceability-table miscount found while editing this block: the table has
    always had 79 (now 70) individual rows, not the 66/57 totals a prior changelog
    entry stated; Coverage now reports the actual row count. Phase 4 total: 26 to
    17 requirements; v1 total: 70 (previously incorrectly stated as 66).*"
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/orthon-lang/docs && for id in CONCEPT-ESS-01 CONCEPT-IMP-01 CONCEPT-DEFER-01 CONCEPT-REJECT-01; do test $(grep -c "$id" .planning/REQUIREMENTS.md) -ge 2 || exit 1; done && test $(grep -cE 'CONCEPT-[0-9]{2}' .planning/REQUIREMENTS.md) -eq 0 && ACTUAL=$(awk '/^## Traceability/,/^\*\*Coverage/' .planning/REQUIREMENTS.md | grep -cE '^\| [A-Z][A-Z0-9-]* \|') && test "$ACTUAL" -eq 70 && grep -q 'v1 requirements: 70 total' .planning/REQUIREMENTS.md && grep -q 'Phase 4 — Derived Features & Decision Pipeline: 17 requirements' .planning/REQUIREMENTS.md && echo PASS</automated>
  </verify>
  <done>CONCEPT-01..13 are gone from REQUIREMENTS.md; CONCEPT-ESS-01/IMP-01/DEFER-01/REJECT-01 each appear in both the Phase 4 section body and the Traceability table; the Traceability table has exactly 70 rows and the Coverage/Phase-summary text agrees with that number; CONTEXT_PARAMETERS.md and REPRESENTATION_MODIFIERS.md are named under CONCEPT-ESS-01 (not lost, not silently reclassified into Phase 2/3).</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 2/3/4/Policy-split caveats to SEED-001 and the research README (stretch)</name>
  <files>.planning/seeds/SEED-001-concept-research-tier-triage.md, how/concepts/research/README.md</files>
  <action>
    In `.planning/seeds/SEED-001-concept-research-tier-triage.md`, insert a new
    paragraph immediately before the "### Essential (Tier 1) — Must-Have (semantic
    bedrock)" heading (do not alter the table itself, its rows, or its "Count: ~30"
    line — the table's `CONTEXT_PARAMETERS.md` / `REPRESENTATION_MODIFIERS.md` rows
    still list them under Important in this static table; that drift from the live
    `essential/` directory is expected and is exactly what this caveat paragraph is
    for — do not move the rows):

    "**2026-07-26 caveat:** This essential-tier table (~30 files at time of writing,
    42 today) is not flat "Phase 4, first pass" material. It splits three ways: ~10
    files feed Phase 2 (Semantic Model), ~10 feed Phase 3 (Primitive Blocks), 3 are
    a separate Policy-level pocket pending relocation (see
    `.planning/todos/pending/move-policy-level-essential-concepts-out-of-pipeline.md`),
    and the remaining ~17-19 stay in Phase 4 alongside `important/` and
    `deferrable/` (this table's row list is also now slightly behind the live
    `essential/`/`important/` directory split — see
    `.planning/REQUIREMENTS.md`'s CONCEPT-ESS-01 for the current file-to-tier
    reality). See `.planning/notes/2026-07-26-tier-vs-phase-mapping.md` for the
    fuller classification. The Important (Tier 2) and Deferrable (Tier 3) tables
    below are unaffected in spirit — they remain entirely Phase 4 material and are
    still the authoritative Phase 4 sequencing source."

    In `how/concepts/research/README.md`, insert a new paragraph immediately after
    the tier table and before the "The `research/` root itself contains only
    meta-files..." paragraph (do not alter the table's rows or its "Phase 4, first
    pass" / "Phase 4, second pass" / "Phase 4, deferred" / "Phase 4, rejection
    decision" column values):

    "**Note (2026-07-26):** `essential/` is not exclusively "Phase 4, first pass."
    Roughly half of its files (~20 of ~42) define one of the six Semantic Model
    dimensions or a primitive construct/composition rule and feed Phase 2 or Phase 3
    directly instead of Phase 4; a further 3 are Policy-level material pending
    relocation out of this pipeline entirely; a further 2 were moved into this tier
    mid-cycle and still await a Phase 2/3/4 classification call. See
    `.planning/notes/2026-07-26-tier-vs-phase-mapping.md` for the split.
    `important/` and `deferrable/` remain entirely Phase 4 material as described
    above."

    Do not redo, reorder, or renumber either file's existing triage tables — this
    task adds context only.
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/orthon-lang/docs && test $(grep -c 'tier-vs-phase-mapping' .planning/seeds/SEED-001-concept-research-tier-triage.md) -ge 1 && test $(grep -c 'tier-vs-phase-mapping' how/concepts/research/README.md) -ge 1 && echo PASS</automated>
  </verify>
  <done>Both SEED-001 and how/concepts/research/README.md reference .planning/notes/2026-07-26-tier-vs-phase-mapping.md in a new caveat paragraph; neither file's existing tables were altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan edits three Markdown planning/documentation files only. No code execution, no external input, no network calls, no package installation occurs. |

## STRIDE Threat Register

No threats identified. This plan has no trust boundary: it is a solo-authored,
offline edit of three Markdown files already tracked in git, with no user input
processed, no code executed, and no dependency installed. STRIDE categories do not
apply to a documentation-only content edit with this shape.
</threat_model>

<verification>
Run, from the repository root (`/Users/mniedre/git/orthon-lang/docs`):

1. `test $(grep -oE "DATA_MODEL\.md|OWNERSHIP\.md|OWNERSHIP_METAPROPERTY\.md|OWNERSHIP_TRANSFER_OPERATOR\.md|MUTABILITY\.md|VALUE_SEMANTICS\.md|IDENTITY_BASED_SAFETY\.md|VISIBILITY_AND_ENCAPSULATION\.md|SCOPED_RESOURCE_LIFECYCLE\.md|EXPRESSION_ORIENTED_LANGUAGE\.md" .planning/REQUIREMENTS.md | sort -u | wc -l) -eq 10` — Phase 2 names all 10 files.
2. `test $(grep -oE "FOUNDATIONAL_ABSTRACTIONS\.md|EXCLUSIVE_DECLARATIONS\.md|STRUCT_AS_VALUE_TYPE\.md|CLASS_WITH_ACT\.md|ACT_AS_FUNCTION\.md|FUNCTIONS\.md|FINAL_BY_DEFAULT\.md|NAMESPACES\.md|DELEGATE\.md|COMPOSITION_OVER_INHERITANCE\.md" .planning/REQUIREMENTS.md | sort -u | wc -l) -eq 10` — Phase 3 names all 10 files.
3. `grep -cE 'CONCEPT-[0-9]{2}' .planning/REQUIREMENTS.md` returns `0` — the old hardcoded 13-item scheme is gone.
4. `awk '/^## Traceability/,/^\*\*Coverage/' .planning/REQUIREMENTS.md | grep -cE '^\| [A-Z][A-Z0-9-]* \|'` returns `70`, matching the Coverage block's stated total.
5. `test $(grep -c 'tier-vs-phase-mapping' .planning/seeds/SEED-001-concept-research-tier-triage.md) -ge 1 && test $(grep -c 'tier-vs-phase-mapping' how/concepts/research/README.md) -ge 1` — both caveat notes present.
6. `grep -c 'CONTEXT_PARAMETERS.md' .planning/REQUIREMENTS.md` returns `>= 1` — the mid-session tier move did not cause this file to be lost from the requirements ledger.
</verification>

<success_criteria>
- REQUIREMENTS.md's Phase 2 and Phase 3 sections each name their 10 real essential-tier source files instead of reading as if authored from scratch.
- REQUIREMENTS.md's Phase 4 section replaces the stale 13-item CONCEPT-01..13 list with 4 tier-scaled requirements (CONCEPT-ESS-01, CONCEPT-IMP-01, CONCEPT-DEFER-01, CONCEPT-REJECT-01) that scale to the real 112-file Phase 4 input, sequenced essential-first.
- The Traceability table, Coverage block, and Phase summary at the bottom of REQUIREMENTS.md are mutually consistent (70 total v1 requirements, Phase 4 at 17), and a pre-existing miscount (66 vs. actual 79/70) is corrected rather than perpetuated.
- The 3 Policy-pocket files and the 2 files a concurrent task moved into essential/ mid-session are preserved with footnotes inside CONCEPT-ESS-01, not silently reclassified into Phase 2/3 or dropped; the 1 remaining borderline important-tier file (DECLARATION_BY_ASSIGNMENT.md) is footnoted on CONCEPT-IMP-01.
- SEED-001 and how/concepts/research/README.md each carry a short caveat pointing at the new split, without their existing triage tables being rewritten.
- ROADMAP.md is explicitly NOT touched by this plan (out of scope per the calling context's constraints, despite being listed in the source todo's "Also touch while in there" section) — a follow-up quick task should apply the same file-list references to ROADMAP.md's Phase 2/3/4 descriptions if desired.
- If tier populations shift again before this plan executes (as they did once already mid-session, via concurrent task 260726-s6t / commit bf0f10a), the executor re-counts `important/` and `deferrable/` at execution time per Task 2's action note, rather than trusting this plan's snapshot numbers blindly.
</success_criteria>

<output>
Create `.planning/quick/260726-s31-rewrite-requirements-md-phase-2-3-4-sect/260726-s31-SUMMARY.md` when done
</output>
