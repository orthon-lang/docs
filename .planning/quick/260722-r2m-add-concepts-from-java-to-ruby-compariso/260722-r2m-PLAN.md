---
phase: quick-260722-r2m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [how/concepts/research/OPEN_CLASSES.md, how/concepts/research/SINGLETON_CLASS.md]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "how/concepts/research/OPEN_CLASSES.md exists as a DRAFT research file matching the DYNAMIC_TYPING.md/EXTENSION_FUNCTIONS.md short-form template (Issue (Why), Examples, Implications for Orthon, Open Questions, Decision History, Cross-References), dated 2026-07-22"
    - "how/concepts/research/SINGLETON_CLASS.md exists as a DRAFT research file with the same short-form template, dated 2026-07-22"
    - "OPEN_CLASSES.md explicitly disambiguates monkey patching (runtime mutation of an existing type's own method table) from FINAL_BY_DEFAULT.md's subclassing-time extensibility question"
    - "SINGLETON_CLASS.md explicitly disambiguates the per-object singleton class/eigenclass from OBJECTS_AND_SINGLETONS.md's per-type single-shared-instance pattern"
    - "OPEN_CLASSES.md cross-references EXTENSION_FUNCTIONS.md as the existing statically-scoped, compile-time-visible alternative mechanism already researched for 'add behaviour to a type I don't own'"
    - "SINGLETON_CLASS.md covers method_missing as part of the same concept, tying its rejection to REFLECTION_ALTERNATIVES.md's existing stance against string-based/non-local dynamic dispatch"
    - "Neither new file modifies any existing file in the repository (additive only) and neither touches what/GLOSSARY.md"
  artifacts:
    - how/concepts/research/OPEN_CLASSES.md
    - how/concepts/research/SINGLETON_CLASS.md
  key_links:
    - "OPEN_CLASSES.md Issue (Why) names and links FINAL_BY_DEFAULT.md, stating the scope difference (definition-time subclassing vs. post-definition method-table mutation)"
    - "OPEN_CLASSES.md Implications for Orthon links EXTENSION_FUNCTIONS.md as the already-researched safe/scoped alternative"
    - "SINGLETON_CLASS.md Issue (Why) names and links OBJECTS_AND_SINGLETONS.md, stating the scope difference (per-instance metaclass vs. per-type single shared instance)"
    - "SINGLETON_CLASS.md Implications for Orthon links METAOBJECTS.md, REFLECTION_ALTERNATIVES.md, and DATA_MODEL.md"
---

<objective>
Add two new DRAFT concept research files to `how/concepts/research/` covering the two
language concepts from the user's Java-to-Ruby comparison table that are genuinely NOT
represented by any existing research file: (1) Open Classes / Monkey Patching — runtime
reopening of an already-defined type's own method table — and (2) the Singleton Class
(Eigenclass) — Ruby's per-object hidden metaclass that lets a single instance receive
methods no sibling instance has, including its `method_missing` fallback-dispatch
mechanism. Every other row in the user's comparison table (duck typing, mixins,
attr_accessor, constructors, packages, freeze/immutability, checked exceptions,
annotations, reflection, builders/DSLs, iterators/streams, generics, switch/case, null
safety, equals/hashCode, primitives-as-objects, static methods, access modifiers) is
already covered by an existing file under a different name and must NOT get a duplicate
file in this plan.

Purpose: This repository's research corpus (100+ files in `how/concepts/research/`) was
cross-checked row-by-row against the comparison table during orchestration. Two concepts
have no dedicated file and no adequate coverage under a different name: monkey patching
(distinct from FINAL_BY_DEFAULT.md's definition-time subclassing question) and the
eigenclass/singleton-class pattern (distinct from OBJECTS_AND_SINGLETONS.md's per-type
single-instance pattern). Both surface consequential questions for Orthon's "comfortable
by construction," "explicit semantics," and "LLM Generability" pillars (why/VISION.md,
why/MANIFESTO.md) that deserve their own DRAFT research artifact ahead of Concept Design
Review — neither should be silently folded into an existing file's scope, and neither
should be invented as a duplicate of work already done.

Output: `how/concepts/research/OPEN_CLASSES.md` and `how/concepts/research/SINGLETON_CLASS.md`,
each a DRAFT research document following the short-form structure established by the most
recent "missing language feature" batch (DYNAMIC_TYPING.md, EXTENSION_FUNCTIONS.md,
REFLECTION_ALTERNATIVES.md): Issue (Why), Examples (comparison table + short code samples),
Implications for Orthon (numbered), Open Questions, Decision History, Cross-References.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@how/concepts/research/DYNAMIC_TYPING.md
@how/concepts/research/EXTENSION_FUNCTIONS.md
@how/concepts/research/FINAL_BY_DEFAULT.md
@how/concepts/research/OBJECTS_AND_SINGLETONS.md
@how/concepts/research/METAOBJECTS.md
@how/concepts/research/REFLECTION_ALTERNATIVES.md
@how/concepts/research/DATA_MODEL.md
@how/concepts/research/COMPOSITION_OVER_INHERITANCE.md
@why/VISION.md
@why/MANIFESTO.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write OPEN_CLASSES.md (open classes / monkey patching)</name>
  <files>how/concepts/research/OPEN_CLASSES.md</files>
  <action>
    Create the file with title `# Open Classes &amp; Monkey Patching`. Directly below the
    title, copy the DRAFT status banner block verbatim in structure and wording from
    `DYNAMIC_TYPING.md`'s header (same warning-emoji glyphs, same "exploratory research
    material for the Concept Design Review process (Milestone 2)" wording, same
    "registered only after acceptance via EDR (Architecture category)" line, same syntax
    note about abstract syntax and Phase 5), but set the "Last updated:" line to 2026-07-22.

    Then write these sections in exactly this order, matching `DYNAMIC_TYPING.md`'s
    heading set (`## Issue (Why)`, `## Examples`, `## Implications for Orthon`,
    `## Open Questions`, `## Decision History`, then a horizontal rule and
    `### Cross-References`) — do not use the longer `_concept.md` template (no
    Principles/Policy Footprint/Model/Default Strategy/Alternative Strategies sections);
    this concept is scoped as a short-form research note like DYNAMIC_TYPING.md and
    EXTENSION_FUNCTIONS.md.

    **`## Issue (Why)`** — Open with the framing question: should an already-defined
    type's own method table be reopenable and mutated at runtime, anywhere in the
    program, without declaring intent at the type's own definition site? Describe Ruby's
    ability to reopen any class — including core/stdlib classes like `String` or
    `Integer` — to add or override methods at any point during program execution, with
    the change visible globally and immediately to every existing and future instance.
    Then explicitly name and link `FINAL_BY_DEFAULT.md`, stating the scope difference in
    its own sentence: that document concerns whether a class can be *subclassed* by
    default at *definition time* (creating a new type from an existing one); this concept
    concerns *runtime redefinition of an already-defined type's own method table* — no
    new type is created, there is no separate namespace, and the change is global and
    immediate. Enumerate the concrete consequences monkey patching introduces: global
    mutable behaviour (a change made in one file alters behaviour everywhere the type is
    used, including inside third-party library internals that never anticipated the
    change); name-collision hazard between two independently-loaded libraries that patch
    the same method on the same type ("monkey patch collision" is a well-documented class
    of Ruby bugs); loss of static verifiability, since no IDE or compiler can determine a
    type's complete method table at any point in the program; a debuggability cost, since
    call-site behaviour no longer matches anything visible at the file containing the
    call; and a library-evolution hazard, since a library author can never assume their
    own public class definition is complete, as any downstream consumer may have patched
    it. Close with the core problem statement: should Orthon permit adding or overriding
    methods on an already-defined type after its definition, and if so, through what
    mechanism and with what visibility/safety boundary?

    **`## Examples`** — A comparison table with columns Language | Reopen/patch
    mechanism | Scope of the change | Compile-time visibility | Safety guard, covering:
    Ruby (reopening any class anywhere, global scope affecting all instances everywhere
    including stdlib, no compile-time visibility, with `refine` as Ruby's own scoped
    opt-in alternative noted as a safety guard); Python (assigning directly to a class
    attribute post-hoc, or module-level patching; global scope, though
    `unittest.mock.patch` provides scoped/temporary patching for tests; no compile-time
    visibility; convention-only safety guard, i.e. "don't monkeypatch stdlib"); JavaScript
    (prototype extension such as assigning a function onto `String.prototype`; global
    scope, the same hazard class as Ruby — "don't extend built-in prototypes" is a
    well-known lint rule; no compile-time visibility; safety guard is community
    convention/linters such as `no-extend-native`); C# (extension methods declared with
    the `this` parameter modifier; scope limited to files that import the extension's
    namespace via `using`, and critically does NOT mutate the original type's own method
    table at all; fully compile-time resolved and visible via the `using` directive; full
    safety guard by construction); Rust (no reopening at all — the orphan rule forbids
    implementing a foreign trait for a foreign type in most cases; disallowed by design;
    compile-time enforced; full safety guard by construction); Kotlin/Swift (extension
    functions/methods, same shape as C# — scoped by import/module visibility, does not
    mutate the original type; compile-time resolved; full safety guard). Follow the table
    with one short Ruby code sample showing `class String` reopened to add a `shout`
    method, and one short contrasting C# code sample showing a `static class` extension
    method achieving the same call-site ergonomics (`str.Shout()`) without touching
    `System.String`'s own method table — note in prose immediately after the C# sample
    that this is the same mechanism already documented in `EXTENSION_FUNCTIONS.md`,
    whose Implication 3 states extension functions are resolved by static receiver type
    with "no vtable modification, no monkey-patching."

    **`## Implications for Orthon`** — Number these implications: (1) global mutable
    behaviour directly conflicts with Orthon's Explicit Semantics pillar and the
    Manifesto's "Semantics over syntax" principle — a method call's behaviour must be
    determinable from the call site and the receiver's declared type, not contingent on
    which files happened to load elsewhere in the program; (2) it also conflicts with the
    LLM Generability pillar (`why/VISION.md` "Designed for the LLM Era" § "Small surface,
    consistent rules") — an LLM generating a call to a builtin or stdlib method cannot
    verify from local context whether that method has been silently redefined elsewhere,
    which is exactly the non-local, context-dependent rule the LLM-readiness pillar warns
    against; (3) Orthon's research already has a scoped, statically-visible answer to the
    legitimate underlying need ("add capability to a type I do not own") —
    `EXTENSION_FUNCTIONS.md`'s extension functions, resolved at compile time by static
    receiver type and imported explicitly per scope — and unrestricted open classes
    should not be introduced as a second, conflicting mechanism for the same need, per the
    Manifesto's "One concept — one syntax"; (4) closed-by-default type definitions
    (`FINAL_BY_DEFAULT.md`) address inheritance-time extensibility, while open classes
    address post-definition mutation of the same type — both should default closed, so
    that a type's method table, once defined, cannot be altered outside its own defining
    module; this is the direct analogue of "closed by default" applied to method-table
    mutation rather than subclassing; (5) if a genuinely scoped, opt-in need exists (for
    example, stubbing a single dependency for one test file), it should be evaluated as a
    distinct, narrowly-scoped mechanism (analogous to Ruby's `refine` or Python's
    `unittest.mock.patch`) rather than unrestricted global monkey patching, and even then
    weighed against whether Extension Functions plus ordinary dependency injection already
    cover the need.

    **`## Open Questions`** — Include at least: (1) should Orthon forbid modifying an
    already-defined type's method table entirely (Rust's orphan-rule-style prohibition),
    relying solely on Extension Functions for "add capability to a type I don't own"?;
    (2) is there a legitimate, narrowly-scoped use case (test mocking/stubbing, a
    monkey-patch limited to a single file) that needs a distinct, explicitly-marked
    mechanism, and if so, does it belong in the core language or in a testing library?;
    (3) how does this interact with Standard Library evolution guarantees — could library
    authors ship "official" extensions to built-in types purely via Extension Functions,
    eliminating any need for a monkey-patch capability?; (4) should there be any
    lexically-scoped exception (Ruby's `refine`) limiting a patch's visibility to the
    file/module that requested it, or does even that violate "one concept, one syntax" by
    introducing a second resolution mode for method calls?

    **`## Decision History`** — Write exactly: "Initial research — no decisions recorded
    yet."

    Then a horizontal rule (`---`) followed by `### Cross-References` with plain bullet
    markdown links (matching `DYNAMIC_TYPING.md`'s bullet-link style, not a checkbox
    list) to: `FINAL_BY_DEFAULT.md`, `EXTENSION_FUNCTIONS.md`, and
    `COMPOSITION_OVER_INHERITANCE.md`.

    Do not modify `what/GLOSSARY.md` or any other existing file.
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/orthon-lang/docs && bash -c 'set -e
F="how/concepts/research/OPEN_CLASSES.md"
test -f "$F"
grep -q "DRAFT" "$F"
grep -q "Last updated:\*\* 2026-07-22" "$F"
ISSUE=$(grep -n "^## Issue (Why)" "$F" | head -1 | cut -d: -f1)
EX=$(grep -n "^## Examples" "$F" | head -1 | cut -d: -f1)
IMPL=$(grep -n "^## Implications for Orthon" "$F" | head -1 | cut -d: -f1)
OPEN=$(grep -n "^## Open Questions" "$F" | head -1 | cut -d: -f1)
HIST=$(grep -n "^## Decision History" "$F" | head -1 | cut -d: -f1)
XREF=$(grep -n "^### Cross-References" "$F" | head -1 | cut -d: -f1)
test -n "$ISSUE" && test -n "$EX" && test -n "$IMPL" && test -n "$OPEN" && test -n "$HIST" && test -n "$XREF"
test "$ISSUE" -lt "$EX"
test "$EX" -lt "$IMPL"
test "$IMPL" -lt "$OPEN"
test "$OPEN" -lt "$HIST"
test "$HIST" -lt "$XREF"
grep -q "FINAL_BY_DEFAULT.md" "$F"
grep -q "EXTENSION_FUNCTIONS.md" "$F"
grep -q "COMPOSITION_OVER_INHERITANCE.md" "$F"
grep -q "refine" "$F"
grep -qi "monkey patch" "$F"
! git diff --name-only -- what/GLOSSARY.md | grep -q GLOSSARY
echo OK'</automated>
  </verify>
  <done>
    `how/concepts/research/OPEN_CLASSES.md` exists with the DRAFT banner dated
    2026-07-22, sections in order Issue (Why), Examples, Implications for Orthon, Open
    Questions, Decision History, Cross-References. The Issue (Why) section names and
    links `FINAL_BY_DEFAULT.md` and states the scope difference (subclassing-time
    extensibility vs. post-definition method-table mutation). The Examples section
    contains the five-language comparison table and Ruby/C# code samples. Implications
    for Orthon links `EXTENSION_FUNCTIONS.md` as the existing safe/scoped alternative.
    No existing file, including `what/GLOSSARY.md`, is modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write SINGLETON_CLASS.md (singleton class / eigenclass)</name>
  <files>how/concepts/research/SINGLETON_CLASS.md</files>
  <action>
    Create the file with title `# Singleton Class (Eigenclass)`. Directly below the
    title, copy the DRAFT status banner block verbatim in structure and wording from
    `DYNAMIC_TYPING.md`'s header (same glyphs, same wording), with the "Last updated:"
    line set to 2026-07-22.

    Then write the same six sections in the same order as Task 1's file (`## Issue
    (Why)`, `## Examples`, `## Implications for Orthon`, `## Open Questions`,
    `## Decision History`, then a horizontal rule and `### Cross-References`) — the
    short-form structure, not the full `_concept.md` template.

    **`## Issue (Why)`** — Open by describing that every Ruby object carries a hidden,
    per-instance class — informally called the "singleton class," also known as the
    "eigenclass" — that sits between the object and its nominal class in the
    method-lookup chain; defining a method directly on one instance (for example
    `def obj.greet; "hi"; end`) inserts that method into the object's own singleton
    class, leaving the object's class and every other instance of that class untouched.
    Note that Ruby uses this exact same mechanism internally to implement per-class
    ("static"/class-level) methods: a class is itself an object, so a class's own
    singleton class holds its class-level methods. Note that the singleton class is also
    the foundation for `method_missing`, a catch-all hook invoked when no matching method
    is found anywhere in the lookup chain (including the singleton class), enabling
    proxies, dynamic DSLs, and "ghost methods" — state that `method_missing` is covered
    as part of this same concept rather than split into a separate file, since it shares
    the same lookup-chain mechanism and the same open questions about non-local dispatch.
    Then explicitly name and link `OBJECTS_AND_SINGLETONS.md`, stating the scope
    difference in its own sentence: that document is about a TYPE having exactly one
    shared global instance (Kotlin's `object` keyword) — "one instance shared by
    everyone" — while this concept is the opposite direction: one SPECIFIC instance
    receives its own private method table, invisible to every other instance of the same
    class. Close with the core problem statement: should Orthon permit per-instance
    method or behaviour attachment independent of the instance's declared type, and
    should any fallback-dispatch hook (a `method_missing` equivalent) exist for calls
    that match no declared method?

    **`## Examples`** — A comparison table with columns Language | Per-instance method
    definition | Mechanism name | Fallback dispatch hook | Typical use, covering: Ruby
    (`def obj.greet; ...; end` or `class &lt;&lt; obj; def greet; ...; end; end`; named
    "singleton class"/"eigenclass"; `method_missing` as the fallback hook; used for DSLs,
    mocking/stubbing individual objects, and implementing class methods internally);
    Python (monkey-patching a bound method onto one instance via
    `types.MethodType(func, obj)`; no dedicated per-object metaclass concept — each
    object shares its class's `__dict__`; `__getattr__` as the fallback hook for missing
    attributes; used for dynamic proxies and mocking libraries); JavaScript (assigning a
    function directly onto one object instance, natural in JS since objects use a
    prototype chain rather than classical per-class dispatch; the mechanism is simply
    "own properties" on the prototype chain; `Proxy` with a `get` trap as the modern
    fallback-dispatch equivalent; used for flexible object composition and proxies);
    Smalltalk (the historical origin of the term "metaclass" — every class already has
    its own singleton metaclass holding class methods, and Ruby generalised this to every
    object, not just classes; mechanism named "metaclass"; `doesNotUnderstand:` as the
    original fallback hook that Ruby's `method_missing` was modelled on; the original
    inspiration for both mechanisms); Java/C#/Kotlin (not supported directly —
    per-instance method addition requires wrapper/decorator objects or delegation
    patterns; no dedicated mechanism name; no equivalent fallback hook, though Java's
    reflection-based `InvocationHandler` for dynamic proxies is the closest analogue; no
    typical per-instance-method use case in idiomatic code). Follow the table with a
    short Ruby code sample showing `def obj.greet; "hi"; end` alongside a brief
    `method_missing` sketch that logs and re-raises for unknown calls, then a short
    contrasting JavaScript sample assigning a function directly onto one object instance
    and a one-sentence note that `Proxy`'s `get` trap is the closest modern
    fallback-dispatch analogue.

    **`## Implications for Orthon`** — Number these implications: (1) per-instance
    method attachment presumes an identity-bearing object model where one instance can
    diverge behaviourally from every sibling instance of the same type; this conflicts
    with `DATA_MODEL.md`'s "Data First" principle (data has no imposed semantic meaning
    until a Data Modifier transforms it) and its "Value semantics by default" principle
    (data is compared by structure, not identity) — the entire premise needs
    re-evaluation once Orthon's actual object/instance model is settled, since it may not
    apply, or may apply only to reference-representation values rather than
    value-semantics data; (2) if any per-instance customisation need is genuinely
    required, Orthon's idiomatic answer is composition — wrap the instance in a new value
    carrying the additional behaviour (`COMPOSITION_OVER_INHERITANCE.md`) — rather than a
    hidden per-object metaclass that mutates behaviour without changing the value's type;
    (3) a `method_missing`-style fallback conflicts with Orthon's Explicit Semantics
    pillar and the Manifesto's "Semantics over syntax" principle, since a reader (human
    or LLM) cannot tell from a call site whether the receiver has a real declared method
    or an intercepted fallback — this is the same non-local, context-dependent behaviour
    `REFLECTION_ALTERNATIVES.md` already rejects for field access ("No `getField(\"name\")`
    pattern... string-based access must always be statically verified"), applied here to
    method dispatch instead of field access; (4) the class-method use case (Ruby's
    internal reliance on the eigenclass to implement `self.method`) is already served in
    Orthon's research by `OBJECTS_AND_SINGLETONS.md`'s companion-object/static-member
    discussion, so no separate per-object mechanism is needed purely to explain
    class-level methods; (5) any legitimate proxy/DSL/mocking need that `method_missing`
    currently serves in Ruby should be routed through Orthon's existing compile-time
    metaprogramming research — `METAOBJECTS.md`'s traits/annotations/macros and
    `REFLECTION_ALTERNATIVES.md`'s sealed-types-plus-pattern-matching — rather than an
    open-ended runtime fallback hook.

    **`## Open Questions`** — Include at least: (1) does Orthon's eventual object model
    (once `DATA_MODEL.md`'s data/behaviour separation is finalised) even have a notion of
    per-instance identity that per-instance method attachment could attach to, or is this
    concept moot by construction for value-semantics data?; (2) if a constrained,
    explicit form of per-instance customisation is wanted (for example, test
    doubles/mocks), should it live in a test-only library feature rather than the core
    language?; (3) is there a legitimate need for an explicit, statically-declared
    "catch-all" trait method (rather than an implicit runtime fallback) to cover the
    DSL/proxy use cases `method_missing` serves in Ruby?; (4) how would a constrained
    reflection API (per `REFLECTION_ALTERNATIVES.md`) cover the legitimate
    mocking/proxying use cases without reintroducing a hidden per-object metaclass?

    **`## Decision History`** — Write exactly: "Initial research — no decisions recorded
    yet."

    Then a horizontal rule (`---`) followed by `### Cross-References` with plain bullet
    markdown links to: `OBJECTS_AND_SINGLETONS.md`, `METAOBJECTS.md`,
    `REFLECTION_ALTERNATIVES.md`, `COMPOSITION_OVER_INHERITANCE.md`, and `DATA_MODEL.md`.

    Do not modify `what/GLOSSARY.md` or any other existing file.
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/orthon-lang/docs && bash -c 'set -e
F="how/concepts/research/SINGLETON_CLASS.md"
test -f "$F"
grep -q "DRAFT" "$F"
grep -q "Last updated:\*\* 2026-07-22" "$F"
ISSUE=$(grep -n "^## Issue (Why)" "$F" | head -1 | cut -d: -f1)
EX=$(grep -n "^## Examples" "$F" | head -1 | cut -d: -f1)
IMPL=$(grep -n "^## Implications for Orthon" "$F" | head -1 | cut -d: -f1)
OPEN=$(grep -n "^## Open Questions" "$F" | head -1 | cut -d: -f1)
HIST=$(grep -n "^## Decision History" "$F" | head -1 | cut -d: -f1)
XREF=$(grep -n "^### Cross-References" "$F" | head -1 | cut -d: -f1)
test -n "$ISSUE" && test -n "$EX" && test -n "$IMPL" && test -n "$OPEN" && test -n "$HIST" && test -n "$XREF"
test "$ISSUE" -lt "$EX"
test "$EX" -lt "$IMPL"
test "$IMPL" -lt "$OPEN"
test "$OPEN" -lt "$HIST"
test "$HIST" -lt "$XREF"
grep -q "OBJECTS_AND_SINGLETONS.md" "$F"
grep -q "METAOBJECTS.md" "$F"
grep -q "REFLECTION_ALTERNATIVES.md" "$F"
grep -q "DATA_MODEL.md" "$F"
grep -qi "method_missing" "$F"
! git diff --name-only -- what/GLOSSARY.md | grep -q GLOSSARY
echo OK'</automated>
  </verify>
  <done>
    `how/concepts/research/SINGLETON_CLASS.md` exists with the DRAFT banner dated
    2026-07-22, sections in order Issue (Why), Examples, Implications for Orthon, Open
    Questions, Decision History, Cross-References. The Issue (Why) section names and
    links `OBJECTS_AND_SINGLETONS.md` and states the scope difference (per-instance
    metaclass vs. per-type single shared instance), and covers `method_missing` within
    the same file. Implications for Orthon links `METAOBJECTS.md`,
    `REFLECTION_ALTERNATIVES.md`, and `DATA_MODEL.md`. No existing file, including
    `what/GLOSSARY.md`, is modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan creates two new static Markdown research documents in a
documentation-only repository — no code, no user input, no external service, no
execution surface. The Ruby/Python/JavaScript/C# snippets shown in each file's Examples
section are illustrative prose content, not executed by any tool in this repository.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick260722r2m-01 | Tampering | how/concepts/research/OPEN_CLASSES.md, how/concepts/research/SINGLETON_CLASS.md | low | accept | Plain-text Markdown creation reviewed via automated grep/heading-order verification in each task's verify step; both files are committed to git for auditability. |
| T-quick260722r2m-02 | Repudiation | Cross-references to FINAL_BY_DEFAULT.md, EXTENSION_FUNCTIONS.md, COMPOSITION_OVER_INHERITANCE.md, OBJECTS_AND_SINGLETONS.md, METAOBJECTS.md, REFLECTION_ALTERNATIVES.md, DATA_MODEL.md | low | accept | All cross-referenced files already exist in the repository (confirmed during planning); each task's automated verify greps for the relevant filenames' presence, preventing dangling or invented references. |
| T-quick260722r2m-03 | Tampering | what/GLOSSARY.md and other existing files (unintended modification) | low | accept | Both tasks are scoped to `<files>` containing only the new file; each verify step confirms `what/GLOSSARY.md` shows no diff, matching the precedent set by the prior C#/root-object/representation-modifiers commits, which did not touch the glossary for DRAFT research files. |

</threat_model>

<verification>
1. `how/concepts/research/OPEN_CLASSES.md` exists with the DRAFT banner (dated
   2026-07-22) and all six sections in order: Issue (Why), Examples, Implications for
   Orthon, Open Questions, Decision History, Cross-References.
2. `how/concepts/research/SINGLETON_CLASS.md` exists with the same structure and date.
3. OPEN_CLASSES.md's Issue (Why) explicitly disambiguates itself from
   FINAL_BY_DEFAULT.md; its Implications for Orthon cross-references
   EXTENSION_FUNCTIONS.md as the existing scoped alternative.
4. SINGLETON_CLASS.md's Issue (Why) explicitly disambiguates itself from
   OBJECTS_AND_SINGLETONS.md and covers `method_missing` in the same file; its
   Implications for Orthon cross-references METAOBJECTS.md, REFLECTION_ALTERNATIVES.md,
   and DATA_MODEL.md.
5. Neither file is a duplicate of any existing research file's scope (verified during
   planning via `grep -ril` across `how/concepts/research/` for "monkey patch", "open
   class", "eigenclass", "singleton class", "method_missing" — no dedicated file found
   prior to this plan).
6. `what/GLOSSARY.md` and all other existing files show no diff (`git status` /
   `git diff --name-only` limited to the two new files).
</verification>

<success_criteria>
- `how/concepts/research/OPEN_CLASSES.md` and `how/concepts/research/SINGLETON_CLASS.md`
  both exist, are DRAFT research documents following the DYNAMIC_TYPING.md/
  EXTENSION_FUNCTIONS.md short-form structure, and are written entirely in English.
- Each file clearly disambiguates its scope from the existing file covering an adjacent
  concept (FINAL_BY_DEFAULT.md for open classes; OBJECTS_AND_SINGLETONS.md for the
  singleton class) rather than duplicating it.
- Each file's Implications for Orthon section grounds its reasoning in Orthon's stated
  pillars (why/VISION.md, why/MANIFESTO.md) and in genuinely related existing research
  (EXTENSION_FUNCTIONS.md, COMPOSITION_OVER_INHERITANCE.md, METAOBJECTS.md,
  REFLECTION_ALTERNATIVES.md, DATA_MODEL.md), not generic restatement.
- No other file in the repository — including what/GLOSSARY.md — is modified by this
  plan.
</success_criteria>

<output>
Create `.planning/quick/260722-r2m-add-concepts-from-java-to-ruby-compariso/260722-r2m-SUMMARY.md` when done
</output>
