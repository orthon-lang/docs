---
phase: 01.1-foundation-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/how/DESIGN_PRINCIPLES.md
  - docs/how/architecture/FITNESS_FUNCTIONS.md
autonomous: true
requirements:
  - PRINC-01
  - FITNESS-01

must_haves:
  truths:
    - DESIGN_PRINCIPLES.md carries a visible closed-for-modification declaration referencing the existing Guiding Rule 2
    - FITNESS_FUNCTIONS.md has 2 new throughput entries (concepts-in-flight, oldest-open-proposal age) following the existing ## Name / **Source:** / **Measured by:** / --- pattern
    - The new fitness functions explicitly state the baseline: 0 concepts in CORE_CONCEPTS.md, N/A oldest proposal
  artifacts:
    - docs/how/DESIGN_PRINCIPLES.md (closed-for-modification block added)
    - docs/how/architecture/FITNESS_FUNCTIONS.md (2 new entries appended)
  key_links:
    - DESIGN_PRINCIPLES.md ↔ DECISION_PROCESS.md (Guiding Rule 2 cross-reference)
    - FITNESS_FUNCTIONS.md new entries ↔ how/concepts/research/ → what/CORE_CONCEPTS.md lifecycle
---

<objective>
Lock DESIGN_PRINCIPLES.md as closed for modification (PRINC-01) and add two throughput fitness functions (FITNESS-01).

Purpose: Fulfill requirements PRINC-01 and FITNESS-01 so Phase 2 (Semantic Model) has a constitutionally-stable principle base and the pipeline has observability metrics.
Output: Two files modified — DESIGN_PRINCIPLES.md (new callout), FITNESS_FUNCTIONS.md (2 new entries appended).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-foundation-completion-consolidate-vision-lock-design-princip/01.1-CONTEXT.md
@.planning/phases/01.1-foundation-completion-consolidate-vision-lock-design-princip/01.1-RESEARCH.md

# Source rule to restate
@docs/how/process/DECISION_PROCESS.md (Guiding Rule 2, lines 46-47)

# Existing entries for format precedent
@docs/how/architecture/FITNESS_FUNCTIONS.md (15 existing entries — follow ## Name / **Source:** / description / **Measured by:** / --- pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1 (D-09): Add closed-for-modification declaration to DESIGN_PRINCIPLES.md</name>
  <files>
    docs/how/DESIGN_PRINCIPLES.md
  </files>
  <action>
    DESIGN_PRINCIPLES.md (551 lines) currently has NO status header — it opens directly with `# Language Design Principles`. Add a closed-for-modification callout.

    Per CONTEXT.md D-09 and RESEARCH.md D-09 map: restate (not invent) the existing rule from DECISION_PROCESS.md Guiding Rule 2. The exact rule text to restate: "DESIGN_PRINCIPLES.md is closed for modification; changes require a Tier 1 EDR."

    Add a `> **🔒 Closed for modification.**` blockquote immediately after the `# Language Design Principles` title (line 1) and before the goals-alignment paragraph. The blockquote should read:

    > **🔒 Closed for modification.**
    > `DESIGN_PRINCIPLES.md` is locked as the project's constitutional design authority. Any future change requires a **Tier 1 EDR** (Architecture category) per [`DECISION_PROCESS.md`](../how/process/DECISION_PROCESS.md) Guiding Rule 2.

    This is a new addition, not an edit to existing text — insert it between the title and the first content paragraph.

    Verification: `grep -n "closed for modification" docs/how/DESIGN_PRINCIPLES.md` returns a match.
  </action>
  <verify>
    <automated>grep -c "closed for modification" docs/how/DESIGN_PRINCIPLES.md | grep -q "^[1-9]"</automated>
  </verify>
  <done>DESIGN_PRINCIPLES.md carries a visible closed-for-modification declaration that cites DECISION_PROCESS.md Guiding Rule 2 and Tier 1 EDR requirement.</done>
</task>

<task type="auto">
  <name>Task 2 (D-10): Add throughput fitness functions to FITNESS_FUNCTIONS.md</name>
  <files>
    docs/how/architecture/FITNESS_FUNCTIONS.md
  </files>
  <action>
    Append 2 new entries to FITNESS_FUNCTIONS.md, following the existing per-entry pattern exactly:

    ```
    ## Concepts In Flight

    **Source:** [Pipeline Throughput Gaps](../../notes/pipeline-throughput-gaps.md) — Gap 4

    Measures how many concepts are currently in the active design pipeline —
    any concept in `how/concepts/research/` that has entered the Concept Design
    Review but has not yet been accepted into `what/CORE_CONCEPTS.md`. A high
    count indicates pipeline congestion; a consistently zero count between
    phases indicates the pipeline may be stalled.

    Acknowledged limitation: the current pipeline has no explicit "review-started"
    marker — this function measures from the lifecycle perspective of
    `how/concepts/research/` (total backlog) → `what/CORE_CONCEPTS.md` (accepted).
    Once Phase 4 begins producing review-started signals, this function should
    be refined to measure in-review concepts specifically.

    **Measured by:** Count of research documents in `how/concepts/research/{essential,important,deferrable,reject}/`
    minus count of accepted entries in `what/CORE_CONCEPTS.md`. Baseline at Phase 1.1:
    132 research files, 0 accepted concepts — this is expected, not a defect, and
    will change once Phase 4 begins accepting concepts.

    ---

    ## Oldest Open Proposal

    **Source:** [TODO.md](../../../TODO.md) — GAP-04

    Tracks the age (in days) of the oldest concept research document that has
    not yet entered the Concept Design Review. A growing age indicates the
    pipeline is not making progress on backlog items.

    **Measured by:** File modification date of the oldest file in
    `how/concepts/research/{essential,important,deferrable,reject}/` that
    has not been accepted into `what/CORE_CONCEPTS.md`. Baseline at Phase 1.1:
    N/A — no concept has been accepted yet, so the "oldest open" is effectively
    the oldest research document's age. Expected to become actionable in Phase 4.
    ```

    Insert these at the end of the file, after the last existing `---` separator and before any trailing content. Follow the exact formatting pattern observed in existing entries (line breaks, spacing, `**Source:**` single-line format).

    Verification: `grep -c "^## " docs/how/architecture/FITNESS_FUNCTIONS.md` should now show 17 (was 15). `grep "Concepts In Flight" docs/how/architecture/FITNESS_FUNCTIONS.md` and `grep "Oldest Open Proposal" docs/how/architecture/FITNESS_FUNCTIONS.md` each return a match.
  </action>
  <verify>
    <automated>test $(grep -c "^## " docs/how/architecture/FITNESS_FUNCTIONS.md) -eq 17</automated>
  </verify>
  <done>Two new entries appended following the existing pattern. Baseline states 0 concepts in flight, N/A oldest proposal. Entry count verification: 17 total.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DESIGN_PRINCIPLES.md → all downstream work | A closed-for-modification declaration that is inconsistent with DECISION_PROCESS.md would create a conflict between the two documents |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1.1-03 | Tampering | DESIGN_PRINCIPLES.md closed-for-modification block | low | mitigate | Restates existing rule; no new authority created. Verify exact wording matches Guiding Rule 2 intent. |
| T-1.1-04 | Tampering | FITNESS_FUNCTIONS.md new entries | low | mitigate | Follows established 15-entry pattern; verify via grep count |
</threat_model>

<verification>
- `grep -n "closed for modification" docs/how/DESIGN_PRINCIPLES.md` — returns match
- `grep -c "^## " docs/how/architecture/FITNESS_FUNCTIONS.md` — equals 17
- `grep -n "Concepts In Flight" docs/how/architecture/FITNESS_FUNCTIONS.md` — returns match
- `grep -n "Oldest Open Proposal" docs/how/architecture/FITNESS_FUNCTIONS.md` — returns match
- `grep -n "0 concepts in flight" docs/how/architecture/FITNESS_FUNCTIONS.md` — baseline stated
</verification>

<success_criteria>
PRINC-01 and FITNESS-01 satisfied. DESIGN_PRINCIPLES.md locked. FITNESS_FUNCTIONS.md has 17 entries with throughput observability. Both done before D-02's acceptance gate or D-04's TODO.md edit reference them.
</success_criteria>

<output>
Create `.planning/phases/01.1-foundation-completion-consolidate-vision-lock-design-princip/01.1-02-SUMMARY.md` when done.
</output>
