---
phase: 01.1-foundation-completion
plan: 04
type: execute
wave: 3
depends_on:
  - 01.1-01
  - 01.1-02
  - 01.1-03
files_modified:
  - docs/TODO.md
autonomous: true
requirements:
  - PROC-03

must_haves:
  truths:
    - GAP-02 is checked off (satisfied by D-01 / PROC-02, now landed)
    - GAP-03 is checked off (satisfied by D-03 / PROC-05, now landed)
    - GAP-04 is checked off (satisfied by D-10 / FITNESS-01, now landed)
    - GAP-01 has a `Req:` cross-reference to CONCEPT-ESS-01 (Phase 4)
    - GAP-05 has a `Req:` cross-reference to SEM-03 (Phase 2)
    - Milestone 0 — Vision both checkboxes are checked off (satisfied by D-06/VISION-01 and D-07/VISION-02)
    - A short "Tracking Model" note exists declaring .planning/REQUIREMENTS.md canonical
  artifacts:
    - docs/TODO.md (GAP-01..05 dispositions, Milestone 0 check-offs, Tracking Model note)
  key_links:
    - TODO.md GAP-01 → .planning/REQUIREMENTS.md CONCEPT-ESS-01 (Phase 4)
    - TODO.md GAP-05 → .planning/REQUIREMENTS.md SEM-03 (Phase 2)
    - TODO.md "Tracking Model" note → .planning/REQUIREMENTS.md
---

<objective>
Finalize the TODO.md tracking model: resolve all 5 Pipeline & Process GAP items against actual Phase 1.1 delivery, check off the two Milestone 0 items now satisfied by Plans 01-03, and add the canonical-source note.

Purpose: Satisfy PROC-03 — convert every GAP item into its appropriate disposition (done/cross-referenced/deferred) so TODO.md stays current. This plan MUST run last, after Plans 01-03 have landed, because GAP-02/03/04 become satisfied by those plans' work.
Output: One file modified (TODO.md).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-foundation-completion-consolidate-vision-lock-design-princip/01.1-CONTEXT.md
@.planning/phases/01.1-foundation-completion-consolidate-vision-lock-design-princip/01.1-RESEARCH.md

# The source file being edited
@docs/TODO.md (lines 1-60 — Pipeline & Process Gaps + Milestone 0)

# Cross-reference targets
@.planning/REQUIREMENTS.md (CONCEPT-ESS-01 at Phase 4, SEM-03 at Phase 2)
</context>

<tasks>

<task type="auto">
  <name>Task 1 (D-04): Resolve GAP-01..05, check off Milestone 0 items, add Tracking Model note</name>
  <files>
    docs/TODO.md
  </files>
  <action>
    Edit TODO.md to apply the following dispositions. This is a multi-edit task; execute edits in order to avoid line-number drift.

    **Structural note before edits:** The TODO.md has these sections relevant to this task:
    - Lines 9-30: "Pipeline & Process Gaps" section (GAP-01..05)
    - Lines 32-39: "Milestone 0 — Vision" section (2 items)
    - Line 40+: "Milestone 1 — Language Inventory" and beyond (not touched by this phase)

    **Edit 1 — "Pipeline & Process Gaps" section header note:** After the existing header blockquote notes (lines 3-7), between the "⚠ Urgent" callout and the GAP-01 item, add a "Tracking Model" blockquote:

    ```
    > **Tracking Model:** `.planning/REQUIREMENTS.md` is the canonical GSD requirement source.
    > Future items should be tracked there. Items below link to their corresponding requirement IDs.
    ```

    **Edit 2 — GAP-01 (line 10-15):** Not satisfied by Phase 1.1 — this requires actually running a concept through the pipeline, which is Phase 4 work. Change `[ ]` to `[ ]` (keep open), append `Req: CONCEPT-ESS-01 (Phase 4)` to the Ref: line. Also update the Ref: line's stale reference: change from the non-existent `CONCEPT-01..13` to the correct `CONCEPT-ESS-01`. The full updated entry:

    ```
    - [ ] **GAP-01:** Run ONE concept through the full pipeline — take `EQUALITY.md` or `DATA_MODEL.md`, push through a 3-step process (Problem → Principle Check → Examples), accept into `CORE_CONCEPTS.md`
      - **Owner:** Solo author
      - **Target:** Phase 4
      - **Ref:** `docs/notes/pipeline-throughput-gaps.md` § Top-5 #1
      - **Req:** CONCEPT-ESS-01 (Phase 4)
    ```

    **Edit 3 — GAP-02 (lines 16-20):** Satisfied by D-01 (this phase, Plan 03). Check off the `[ ]` → `[x]`. Append `Req: PROC-02` to the Ref: line. Change Target: to the completion date "Phase 1.1" (or remove, noting it's done). The full updated entry:

    ```
    - [x] **GAP-02:** Collapse the 11-stage concept design review to a 5-step pipeline suitable for solo authorship; reserve full gate mechanism for retroactive refinement
      - **Owner:** Solo author
      - **Target:** Phase 1.1
      - **Ref:** `docs/notes/pipeline-throughput-gaps.md` § Top-5 #2, § Minimal Pipeline
      - **Req:** PROC-02
    ```

    **Edit 4 — GAP-03 (lines 21-25):** Satisfied by D-03 (this phase, Plan 03). Note: the document already existed — D-03 completed the "update README.md" part. Check off `[ ]` → `[x]`. Append `Req: PROC-05`. Change Target: to "Phase 1.1":

    ```
    - [x] **GAP-03:** Create `DECISION_PROCESS.md` — one-page decision authority extracted from `AGENTS.md` and `concept-design-review.md`; update `README.md` and `AGENTS.md` to reference it
      - **Owner:** Solo author
      - **Target:** Phase 1.1
      - **Ref:** `docs/notes/pipeline-throughput-gaps.md` § Top-5 #3
      - **Req:** PROC-05
    ```

    **Edit 5 — GAP-04 (lines 26-30):** Satisfied by D-10 (this phase, Plan 02). Check off `[ ]` → `[x]`. Append `Req: FITNESS-01`. Change Target: to "Phase 1.1":

    ```
    - [x] **GAP-04:** Add throughput fitness functions to `FITNESS_FUNCTIONS.md` — minimum: concepts-in-flight, oldest open proposal age
      - **Owner:** Solo author
      - **Target:** Phase 1.1
      - **Ref:** `docs/notes/pipeline-throughput-gaps.md` § Top-5 #4
      - **Req:** FITNESS-01
    ```

    **Edit 6 — GAP-05 (lines 31-35):** Honored by D-05 (this phase's "no new EDR" constraint) but the literal verification (a language-design EDR gets written next) happens in Phase 2. Keep open `[ ]` → `[ ]` (or change to `[ ]`), append `Req: SEM-03 (Phase 2)`. Specifically, GAP-05's guidance ("the next EDR must be a language design decision, not meta-process") is honored by this phase's restraint (D-05), but the actual next EDR event is Phase 2's SEM-03. Recommended disposition: check off since the guidance is honored, but add the Phase 2 cross-reference for traceability:

    ```
    - [x] **GAP-05:** The next EDR must be a language design decision (not meta-process) — reorient the EDR system to its stated purpose
      - **Owner:** Solo author
      - **Target:** Phase 2
      - **Ref:** `docs/notes/pipeline-throughput-gaps.md` § Top-5 #5
      - **Req:** SEM-03 (Phase 2)
    ```

    **Edit 7 — Milestone 0 — Vision (lines 38-44):** Both items are satisfied by D-06 (VISION-01) and D-07 (VISION-02) respectively, which landed in Plan 01. Check off both `[ ]` → `[x]`:

    ```
    - [x] review Manifesto (`docs/why/MANIFESTO.md`), Vision (`docs/why/VISION.md`), Zen (`docs/why/ZEN.md`) using Working Backwards rationale (`docs/why/WORKING_BACKWARDS.md`)
      - **Owner:** Solo author
      - **Target:** Phase 1.1
    - [x] validate criteria-based algorithm selection idea (`docs/notes/criteria-based-algorithm-selection.md`)
      - **Owner:** Solo author
      - **Target:** Phase 1.1
    ```

    Update each item's Target: from "Phase 2" to "Phase 1.1" since that's when they were actually resolved.

    **Important: Do NOT touch items below "Milestone 1 — Language Inventory" (lines 46+).** Those are valid future-phase items that remain open.

    Verification:
    - GAP-02 `Req: PROC-02` line exists and is checked: `grep -A4 "GAP-02" docs/TODO.md | grep "\[x\]"`
    - GAP-03 `Req: PROC-05` line exists and is checked: `grep -A4 "GAP-03" docs/TODO.md | grep "\[x\]"`
    - GAP-04 `Req: FITNESS-01` line exists and is checked: `grep -A4 "GAP-04" docs/TODO.md | grep "\[x\]"`
    - GAP-01 `Req: CONCEPT-ESS-01` line exists: `grep "CONCEPT-ESS-01" docs/TODO.md`
    - GAP-05 `Req: SEM-03` line exists: `grep "SEM-03" docs/TODO.md`
    - Milestone 0 items checked: `grep -A1 "review Manifesto" docs/TODO.md | grep "\[x\]"` and `grep -A1 "validate criteria" docs/TODO.md | grep "\[x\]"`
    - Tracking Model note exists: `grep "Tracking Model" docs/TODO.md`
  </action>
  <verify>
    <automated>grep -c "\[x\]" docs/TODO.md | grep -q "^[1-9]" && grep "CONCEPT-ESS-01" docs/TODO.md | grep -q "Req" && grep "SEM-03" docs/TODO.md | grep -q "Req" && grep "Tracking Model" docs/TODO.md | grep -q "REQUIREMENTS"</automated>
  </verify>
  <done>GAP-02/03/04 checked off with Req cross-references. GAP-01 cross-referenced to CONCEPT-ESS-01 (Phase 4). GAP-05 checked off with Req to SEM-03 (Phase 2). Both Milestone 0 items checked off. Tracking Model note present. Unrelated TODO.md items untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TODO.md → project tracking | Incorrect disposition (e.g., checking off something not actually done) would create a stale checkbox in the opposite direction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1.1-08 | Tampering | TODO.md GAP dispositions | low | mitigate | Each checked-off GAP item must have corresponding Req: field; verify via grep that Plans 01-03 SUMMARYs confirm each prerequisite landed |
</threat_model>

<verification>
- `grep "CONCEPT-ESS-01" docs/TODO.md` — GAP-01 correctly cross-referenced to Phase 4
- `grep "\[x\]" docs/TODO.md | wc -l` — at least 5 items checked off (GAP-02/03/04/05 + 2 Milestone 0 = 5 total)
- Each checked GAP has a matching Req: line
- `grep "SEM-03" docs/TODO.md` — GAP-05 correctly cross-referenced to Phase 2
- `grep "Tracking Model" docs/TODO.md` — canonical source note present
- Milestone 1+ items untouched (verify their `[ ]` remain open)
</verification>

<success_criteria>
PROC-03 satisfied. TODO.md is current: 3 GAP items checked off as satisfied by this phase, 1 cross-referenced to Phase 4, 1 checked off with Phase 2 cross-reference. Both Milestone 0 items checked off. Tracking Model note declares REQUIREMENTS.md canonical.
</success_criteria>

<output>
Create `.planning/phases/01.1-foundation-completion-consolidate-vision-lock-design-princip/01.1-04-SUMMARY.md` when done.
</output>
