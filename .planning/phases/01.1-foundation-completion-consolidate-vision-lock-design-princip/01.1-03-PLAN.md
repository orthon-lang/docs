---
phase: 01.1-foundation-completion
plan: 03
type: execute
wave: 2
depends_on:
  - 01.1-01
  - 01.1-02
files_modified:
  - docs/how/concept-design-review.md
  - docs/how/CONCEPT_PIPELINE.md
  - docs/how/PROCESS_INVENTORY.md
  - docs/what/concepts/README.md
  - docs/how/concepts/README.md
  - docs/when/ROADMAP.md
  - docs/how/DOCUMENTATION_PRINCIPLES.md
  - docs/how/process/DECISION_PROCESS.md
  - docs/how/process/DECISION_PIPELINE.md
  - README.md
autonomous: true
requirements:
  - PROC-01
  - PROC-02
  - PROC-04
  - PROC-05

must_haves:
  truths:
    - concept-design-review.md has a 3-5 step pipeline (not 12 steps) and no stale step-count references in its own body
    - Every stale "11-step/11-stage" cross-reference across 7 files (ROADMAP.md, REQUIREMENTS.md, CONCEPT_PIPELINE.md ×2, PROCESS_INVENTORY.md, what/concepts/README.md, how/concepts/README.md) is corrected
    - No stale "11-step" text remains anywhere outside decision_records/ and notes/
    - concept-design-review.md has an ## Acceptance Gate section defining DRAFT→Accepted transition, owner, criteria, EDR linkage, and referencing LLM Generability Gate
    - DOCUMENTATION_PRINCIPLES.md has a template-compliance checklist alongside its existing Section Structure list
    - DECISION_PROCESS.md status is Accepted (not DRAFT)
    - DECISION_PIPELINE.md status is finalized (not ⚠ Placeholder)
    - README.md "Getting Started" table has a row for DECISION_PROCESS.md
  artifacts:
    - docs/how/concept-design-review.md (collapsed + acceptance gate)
    - docs/how/CONCEPT_PIPELINE.md (step-count fixes)
    - docs/how/PROCESS_INVENTORY.md (step-count fix)
    - docs/what/concepts/README.md (step-count fix)
    - docs/how/concepts/README.md (step-count fix)
    - docs/when/ROADMAP.md (step-count fix)
    - docs/how/DOCUMENTATION_PRINCIPLES.md (checklist added)
    - docs/how/process/DECISION_PROCESS.md (status flipped to Accepted)
    - docs/how/process/DECISION_PIPELINE.md (banner finalized)
    - README.md (new table row)
  key_links:
    - concept-design-review.md (acceptance gate) ↔ llm-native-concept-shortlist.md (LLM Generability Gate criteria)
    - concept-design-review.md ↔ DOCUMENTATION_PRINCIPLES.md (template-compliance cross-ref)
    - DECISION_PROCESS.md status → README.md reference
---

<objective>
Transform the concept design pipeline and finalize process infrastructure: collapse concept-design-review.md, fix all stale step-count cross-references, add the acceptance gate and template-compliance checklist, and flip process doc statuses.

Purpose: Satisfy PROC-01 (acceptance gate), PROC-02 (collapse), PROC-04 (checklist), PROC-05 (status flips). This plan depends on Plan 01 (D-08 shortlist) and Plan 02 (D-09 lock) having landed first — the acceptance gate references the LLM Generability Gate from the shortlist, and conceptually assumes DESIGN_PRINCIPLES.md's closed-for-modification rule is already stated in its canonical location.
Output: 9 files modified (1 collapsed, 5 cross-reference fixes, 1 checklist added, 2 status flips, 1 README row).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-foundation-completion-consolidate-vision-lock-design-princip/01.1-CONTEXT.md
@.planning/phases/01.1-foundation-completion-consolidate-vision-lock-design-princip/01.1-RESEARCH.md

# Reference: new shortlist created in Plan 01
@docs/notes/llm-native-concept-shortlist.md

# Reference: DESIGN_PRINCIPLES.md lock created in Plan 02
@docs/how/DESIGN_PRINCIPLES.md (closed-for-modification block)

# Template for acceptance gate criteria
@docs/notes/llm-generability-gate.md (5-criteria table, lines 69-75)

# Existing template structure
@docs/how/templates/_concept.md (8-section template that checklist audits against)
</context>

<tasks>

<task type="auto">
  <name>Task 1 (D-01): Collapse concept-design-review.md and fix all stale "11-step" cross-references</name>
  <files>
    docs/how/concept-design-review.md
    docs/how/CONCEPT_PIPELINE.md
    docs/how/PROCESS_INVENTORY.md
    docs/what/concepts/README.md
    docs/how/concepts/README.md
    docs/when/ROADMAP.md
  </files>
  <action>
    This task has two sub-parts that MUST execute together since they are listed in D-01 as "the same pass."

    **Part A: Collapse concept-design-review.md (the primary edit)**

    The document currently has a 12-step procedure (12 numbered rows in the step table + 12 "### N." detail subsections). Replace this with a 5-step pipeline modeled on `docs/notes/pipeline-throughput-gaps.md`'s "Minimal Pipeline."

    CRITICAL reconciliation per RESEARCH.md: The source note calls the pipeline "five steps" but lists 7 tokens (Idea → Problem Description → Minimal Solution → Principle Check → Examples → EDR → CORE_CONCEPTS.md). Follow the same document's existing Procedure/Output boundary split:
    - **5 numbered procedure steps:** Idea/Problem, Minimal Solution, Principle Check, Examples, EDR
    - **CORE_CONCEPTS.md registration** stays in the existing `## Output` section (not a 6th step)
    - This matches the existing document's structure (it already has a separate Output section for deliverables)

    What to change in concept-design-review.md (exact edits):
    1. **Line 3:** Update header description: "The 12-step procedure" → "The 5-step procedure"
    2. **Line 18:** "undergoes a uniform 12-step procedure" → "undergoes a uniform 5-step procedure"
    3. **Line 38 (Relationship to Other Documents table):** Update the table cell from "12-step procedure (this document)" to "5-step procedure (this document)"
    4. **Line 44:** "The 12-step procedure produces a concept design" → "The 5-step procedure produces a concept design"
    5. **Lines 66-79 (step table):** Replace the 12-row table with a 5-row table:
       ```
       | # | Step | Purpose | Answer |
       |---|------|---------|--------|
       | 1 | Idea/Problem | What problem does this concept solve? | Problem description |
       | 2 | Minimal Solution | What is the simplest valid solution? | Solution sketch |
       | 3 | Principle Check | Which Design Principles does it satisfy? | Principle mapping |
       | 4 | Examples | Show all canonical forms | Examples list |
       | 5 | EDR | Record the decision with rationale | EDR file |
       ```
    6. **Lines 83-202 (detail subsections):** Replace the twelve "### N." detail subsections with five new ones matching the new step names above. Each should be substantially shorter (3-5 lines of guidance each, not the current multi-paragraph depth). Format:
       ```
       ### 1. Idea / Problem
       [1-2 sentence guidance: What problem does this concept solve for the programmer? State the gap or friction. If it solves no independently-statable problem, reject at this step.]

       ### 2. Minimal Solution
       [1-2 sentence guidance: Describe the simplest valid solution. What is the minimum semantic addition that solves the problem? If the solution requires multiple simultaneous additions, it is not orthogonal — split the concept.]

       ### 3. Principle Check
       [1-2 sentence guidance: Map the concept to DESIGN_PRINCIPLES.md. Which principles does it satisfy? Does it violate any principle? If it violates any closed-for-modification principle, the concept requires a Tier 1 EDR and is ineligible for solo-author acceptance.]

       ### 4. Examples
       [1-2 sentence guidance: Write all canonical forms — every equivalent way to express this feature. Follow the "Show All Canonical Forms" principle from DESIGN_PRINCIPLES.md. Examples precede semantic explanation.]

       ### 5. EDR
       [1-2 sentence guidance: Create an Engineering Decision Record (Architecture category) accepting the concept. Use the _edr-architecture.md template. The EDR must cite the Problem, Solution, and Principle Check from steps 1-3.]
       ```
    7. **Lines 206-220 (Validation section):** Update "After the 12-step procedure is complete" → "After the 5-step procedure is complete"
    8. **Lines 223-232 (Output section):** Add a bullet for CORE_CONCEPTS.md registration (it already lists "Concept document" and "EDR" — add a third: "- **CORE_CONCEPTS.md registration** — the concept is entered into the accepted-concept registry at `what/CORE_CONCEPTS.md`")
    9. Leave the "How to Maintain This Document" section unchanged — it already has generic advisory text about updating the table and cross-references if steps change.

    **Part B: Fix stale "11-step" / "11-stage" cross-references in 5 + 2 files**

    Do NOT edit files under `how/decision_records/` (historical EDRs) or `docs/notes/pipeline-throughput-gaps.md` (historical analysis note).

    **5 explicitly-named cross-references (per D-01):**
    1. `.planning/ROADMAP.md` line 78: `**11-step** procedure` → `**5-step** procedure` (the collapsed target, not the current state — post-collapse)
    2. `.planning/REQUIREMENTS.md` line 36 (PROC-02): `current **11-step** procedure` → `current **12-step** procedure` (this describes the state *before* collapse, i.e. accurately describes pre-collapse — wait, actually the RESEARCH.md says to consult the current text.)

    Let me re-read. REQUIREMENTS.md line 36 says: "- [ ] **PROC-02**: Concept Design Review process fully documented and collapsed from its current **11-step** procedure to a 3-5 step pipeline for solo authorship". This is the stale "11" that needs fixing. But what should it say? After collapse, it should say "collapsed to its current 5-step procedure". Since this is the requirement description, it describes the goal state.

    Actually, let me think about this more carefully. The REQUIREMENTS.md entry for PROC-02 will be satisfied by this phase. So the `[ ]` will be checked off, and the text should describe the final state. Change to: `current **11-step** procedure` → `former **12-step** procedure`.

    Wait, but D-01 says "the stale 11-step count is corrected to the accurate 12-step description" in these cross-references. So the fix is: wherever "11-step/11-stage" appears referring to the pre-collapse state, change to "12-step/12-stage" (the accurate pre-collapse count). Where it refers to the post-collapse target, change to "5-step."

    Let me be precise about each file:

    1. `.planning/ROADMAP.md` line 78: `**11-step** procedure` → this refers to the pre-collapse state as described in the ROADMAP's success criteria. Change to `**12-step** procedure` (the accurate pre-collapse description).
    2. `.planning/REQUIREMENTS.md` line 36: `current **11-step** procedure` → change to `former **12-step** procedure` (accurate pre-collapse description, past tense since post-collapse)
    3. `docs/how/CONCEPT_PIPELINE.md` line 46 (ASCII diagram): `(11 steps)` → `(12 steps)` and update step names to match original: `1. Problem  2. Use Cases 3. Necessity... 12. EDR` — BUT since the diagram shows the old pipeline which no longer exists post-collapse, the entire diagram should arguably be updated to reflect the new pipeline. However, CONCEPT_PIPELINE.md is a more comprehensive document that describes the overall concept pipeline, not just the review step. The diagram at line 46 is a bird's-eye view. For now, fix the count and step names to reflect pre-collapse state (12 steps with accurate names) since the diagram is deprecated and will be rewritten in full when CONCEPT_PIPELINE.md is next revised.
    
    Actually, wait. The diagram shows `(11 steps)` with 11 step names. After the collapse, this diagram is no longer accurate. But the task is just to fix the stale count. Let me change `(11 steps)` to `(12 steps)` and keep the step names as they are (or update to 12 matching the original document's names). The diagram is schematic; fine to just fix the number.

    Actually, looking at RESEARCH.md more carefully: `CONCEPT_PIPELINE.md` line 46 has an ASCII diagram: `│ (11 steps)                  │      1. Problem         7. Interactions` — this is inside a box-drawing diagram. The diagram lists old step names. Since the collapse makes the old diagram obsolete, the simplest fix is to update the number from 11 to 12 (correcting the count to match the pre-collapse state accurately) and leave the step names as-is since the whole diagram will need replacement when CONCEPT_PIPELINE.md is updated more comprehensively.

    Hmm, but actually this diagram is part of CONCEPT_PIPELINE.md which is a higher-level document. Let me just fix the count as instructed and not try to rewrite the diagram.

    4. `docs/how/CONCEPT_PIPELINE.md` line 185: `### 5. Concept Design Review (11 Steps)` → `### 5. Concept Design Review (5 Steps)` (post-collapse — this section header should reflect the new collapsed state since it describes the current process)
    5. `docs/how/CONCEPT_PIPELINE.md` line 189: `Each concept undergoes 11 steps in order` → `Each concept undergoes 5 steps in order`
    6. `docs/how/CONCEPT_PIPELINE.md` line 203: `After the 11-step design, the concept is evaluated through 7 independent` → `After the 5-step design, the concept is evaluated through 7 independent` (note: this sentence is about the validation gates stage, not the review stage — still needs the number fixed, but the "7 independent" gates are a different number unrelated to the step count)
    7. `docs/how/PROCESS_INVENTORY.md` line 30 (table cell): `Concept Design Review (11-step)` → `Concept Design Review (5-step)` (post-collapse — describes the current state after this phase)
    8. `docs/what/concepts/README.md` line 13: `full **11-stage** lifecycle` → `full **5-stage** lifecycle` (post-collapse — describes the pipeline after this phase)

    **2 discovered cross-references (per RESEARCH.md Open Question 1 — include for consistency with Phase 1 precedent):**
    9. `docs/how/concepts/README.md` line 16: `full **11-stage** lifecycle — research inbox, decision pipeline, primitive decomposition, layer classification, concept design review, validation gates, language design gate, EDR, accepted draft, cross-cutting review, and registry entry` → Change `**11-stage**` to `**5-stage**` AND update the lifecycle list to match the collapsed pipeline: `research inbox, decision pipeline, concept design review, EDR, and registry entry`
    
    Wait, do I need to update the full lifecycle list? The RESEARCH.md says "Same stale-count pattern, same file family (concepts/README.md short-form)". The minimum fix D-01 requires is the step count. But if I only fix the number from 11 to 5, the list that follows still enumerates 11 stages. Let me keep the section count fix to match the narrowed scope of D-01 and leave the enumerated items — they describe the broader lifecycle approach (including validation gates, language design gate, etc.) which still exist as separate processes. The 5-stage Concept Design Review collapse is about the design review itself, not about eliminating the gate layers.

    Actually, `docs/how/concepts/README.md` line 16 says "11-stage lifecycle — research inbox, decision pipeline, primitive decomposition, layer classification, concept design review, validation gates, language design gate, EDR, accepted draft, cross-cutting review, and registry entry." That's actually a description of the overall concept lifecycle which has more than 5 stages (it includes pre-review, review, gate, and post-review stages). So the "11-stage" there refers to the total lifecycle stages, not just the Concept Design Review steps. Changing it to "5-stage" would be misleading.

    The safest fix is the minimal one: just update the number to be accurate. Since the total lifecycle has more than 11 stages, the "11" is stale but the fix isn't simple. RESEARCH.md flagged this for planner judgment. Let me take the conservative approach: change `**11-stage**` to `**multi-stage**` in `docs/how/concepts/README.md` — this avoids both the stale count and the misleading "5" when the lifecycle has more steps. This is more honest than picking a wrong number.

    For `docs/when/ROADMAP.md` line 99: `- Collapse **11-step** Concept Design Review → 5-step pipeline for solo author` — This is the original architectural-design-hierarchy roadmap (distinct from `.planning/ROADMAP.md`). Change `**11-step**` → `**12-step**` (the accurate pre-collapse description — the collapse target was always 5-step, as this line already correctly states).

    Do NOT touch: `docs/notes/pipeline-throughput-gaps.md` (historical analysis), `docs/TODO.md` (handled in D-04), any file under `how/decision_records/`.

    Verification of Part A: `grep -n "^### [1-5]\. " docs/how/concept-design-review.md` shows exactly 5 numbered steps.
    Verification of Part B: `grep -rn "11-step\|11-stage\|11 step\|11 stage" .planning/ROADMAP.md .planning/REQUIREMENTS.md docs/how/CONCEPT_PIPELINE.md docs/how/PROCESS_INVENTORY.md docs/what/concepts/README.md docs/how/concepts/README.md docs/when/ROADMAP.md` returns zero matches.
  </action>
  <verify>
    <automated>grep -rn "11-step\|11-stage\|11 step\|11 stage" .planning/ROADMAP.md .planning/REQUIREMENTS.md docs/how/CONCEPT_PIPELINE.md docs/how/PROCESS_INVENTORY.md docs/what/concepts/README.md docs/how/concepts/README.md docs/when/ROADMAP.md 2>/dev/null; test $? -eq 1</automated>
  </verify>
  <done>concept-design-review.md collapsed to 5 steps. All 7 cross-reference files have accurate step counts. No stale "11-step/11-stage" text remains outside decision_records/ and notes/.</done>
</task>

<task type="auto">
  <name>Task 2 (D-02): Add acceptance gate to concept-design-review.md and template-compliance checklist to DOCUMENTATION_PRINCIPLES.md</name>
  <files>
    docs/how/concept-design-review.md
    docs/how/DOCUMENTATION_PRINCIPLES.md
  </files>
  <action>
    **Part A: Acceptance gate in concept-design-review.md (PROC-01)**

    Insert a new `## Acceptance Gate` section between the existing `## Output` section (last line ~232) and `## How to Maintain This Document` (starts ~line 235). Content must define:

    - **Purpose:** The DRAFT → Accepted transition for language concepts.
    - **Owner:** Solo Author (per TODO.md convention).
    - **Criteria for DRAFT → Accepted:**
      1. The concept has completed all 5 steps of the Concept Design Review (above).
      2. An EDR (Architecture category) exists accepting the concept, using the `_edr-architecture.md` template.
      3. The concept has passed the **LLM Generability Gate** (see [LLM-Native Concept Shortlist](../notes/llm-native-concept-shortlist.md)) — the 5 criteria (Schema-serializable, Predictable generation ≥90%, No hallucination surface, Strategy-aware default, Self-correctable) from `llm-generability-gate.md` are evaluated and documented in the EDR.
      4. A template-compliance check confirms the concept document follows the 8-section `_concept.md` template (see [DOCUMENTATION_PRINCIPLES.md](../DOCUMENTATION_PRINCIPLES.md) § Section Structure).
      5. The concept is registered in `what/CORE_CONCEPTS.md` as an accepted concept.
    - **EDR Linkage:** The EDR must cite the Problem, Solution, and Principle Check from steps 1-3 of the review, and must reference the LLM Generability Gate criteria results.

    **Part B: Template-compliance checklist in DOCUMENTATION_PRINCIPLES.md (PROC-04)**

    Add a checklist-form rendering of the existing 8-section list directly after the existing 8-section enumerated list. The checklist reuses the exact section names already defined in the document (Issue/Why, Principles, Policy Footprint, Model/What, Default Strategy, Alternative Strategies, Open Questions, Decision History). Format:

    ```markdown
    ### Template-Compliance Audit Checklist

    When reviewing a concept document for acceptance, verify each section is present and complete:

    - [ ] **1. Issue (Why)** — problem statement is clear and independently stateable
    - [ ] **2. Principles** — constraints that cannot be violated are listed
    - [ ] **3. Policy Footprint** — which Policy Types the concept involves are identified
    - [ ] **4. Model (What)** — language model and semantics are defined
    - [ ] **5. Default Strategy** — default implementation behaviour is specified
    - [ ] **6. Alternative Strategies** — permitted implementation variants are described
    - [ ] **7. Open Questions** — unresolved issues are listed
    - [ ] **8. Decision History** — rejected alternatives with rationale are recorded
    ```

    Place this after the existing note block (`> **Note:** Previous versions of this document...`) and before the next `##` heading.

    Verification of Part A: `grep -n "## Acceptance Gate" docs/how/concept-design-review.md` returns a match. `grep -n "LLM Generability Gate" docs/how/concept-design-review.md` returns a match.
    Verification of Part B: `grep -n "Template-Compliance Audit Checklist" docs/how/DOCUMENTATION_PRINCIPLES.md` returns a match.
  </action>
  <verify>
    <automated>grep -c "## Acceptance Gate" docs/how/concept-design-review.md | grep -q "^[1-9]" && grep -c "LLM Generability Gate" docs/how/concept-design-review.md | grep -q "^[1-9]" && grep -c "Template-Compliance Audit Checklist" docs/how/DOCUMENTATION_PRINCIPLES.md | grep -q "^[1-9]"</automated>
  </verify>
  <done>Acceptance gate section exists in concept-design-review.md referencing LLM Generability Gate criteria and 8-section template compliance. DOCUMENTATION_PRINCIPLES.md has the audit checklist.</done>
</task>

<task type="auto">
  <name>Task 3 (D-03): Finalize DECISION_PROCESS.md and DECISION_PIPELINE.md; add README.md reference</name>
  <files>
    docs/how/process/DECISION_PROCESS.md
    docs/how/process/DECISION_PIPELINE.md
    README.md
  </files>
  <action>
    **Part A: DECISION_PROCESS.md — status flip + light content review**

    1. Read the full file (52 lines) to confirm content hasn't drifted (Decision Tiers table, Recording rules, 4 Guiding Rules). RESEARCH.md already confirmed coherence — no drift found. If the content is stale or incorrect, fix inline but do not restructure.
    2. Edit the status line (line 5): `> **Status:** DRAFT — created during Phase 1 (Foundation).` → `> **Status:** Accepted`
    3. Verify that `> **See also:**` references still resolve to existing files.

    **Part B: DECISION_PIPELINE.md — finalized banner**

    1. Read the full file (58 lines) to confirm the 10-question list and Decision Flow diagram are coherent. RESEARCH.md confirmed content is complete. If stale, fix inline.
    2. Edit the banner (line 3): `> **⚠️ DRAFT — Placeholder for Phase 4.**` → `> **✅ Finalized — content ready for use.**`
    3. Edit the second status line (line 6): `> **Status:** Placeholder — to be filled during Phase 4 of M1.` → `> **Status:** Accepted`
    4. The `## Pipeline Application` section (lines 55-57) correctly stays empty — its HTML comment `<!-- To be filled during Phase 4 — one entry per feature processed -->` is preserved. Optionally add a clarifying note: `> **Note:** This log is intentionally empty. It will be populated during Phase 4 (Derived Features & Decision Pipeline) as each feature is processed through the pipeline.`

    **Part C: README.md — add DECISION_PROCESS.md reference**

    1. Read README.md (74 lines, repo root at `/Users/mniedre/git/orthon-lang/README.md`).
    2. Add a new row to the "Getting Started" table (existing table is at lines 48-57) for DECISION_PROCESS.md, following the exact existing row syntax:
       ```markdown
       | [Decision Process](how/process/DECISION_PROCESS.md) | One-page decision authority — who decides what, using which criteria |
       ```
       Insert this row after the "Decision Records" row (last row of the table currently), or in alphabetical/natural position.

    Verification:
    - `grep -n "Status:" docs/how/process/DECISION_PROCESS.md` — shows "Accepted", not "DRAFT"
    - `grep -n "Status:" docs/how/process/DECISION_PIPELINE.md` — shows "Accepted", not "Placeholder"
    - `grep -n "DECISION_PROCESS" README.md` — returns a match (currently zero, should become 1)
  </action>
  <verify>
    <automated>grep -c "Accepted" docs/how/process/DECISION_PROCESS.md | grep -q "^[1-9]" && grep -c "Finalized\|Accepted" docs/how/process/DECISION_PIPELINE.md | grep -q "^[1-9]" && grep -c "DECISION_PROCESS" README.md | grep -q "^[1-9]"</automated>
  </verify>
  <done>DECISION_PROCESS.md status is Accepted. DECISION_PIPELINE.md is finalized with preserved empty Pipeline Application log. README.md "Getting Started" table includes a DECISION_PROCESS.md row. AGENTS.md already references it — no change needed there.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concept-design-review.md → all concept designers | This is the canonical procedure document; errors here propagate to all future concept work |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1.1-05 | Tampering | concept-design-review.md collapse | low | mitigate | Step count verification via grep; acceptance gate cross-refs checked post-commit |
| T-1.1-06 | Repudiation | Stale "11-step" cross-refs | low | mitigate | Automated grep for "11-step\|11-stage" across 7 target files returns zero |
| T-1.1-07 | Tampering | README.md reference | low | mitigate | Verify DECISION_PROCESS appears in Getting Started table |
</threat_model>

<verification>
- `grep -rn "11-step\|11-stage\|11 step\|11 stage" .planning/ROADMAP.md .planning/REQUIREMENTS.md docs/how/CONCEPT_PIPELINE.md docs/how/PROCESS_INVENTORY.md docs/what/concepts/README.md docs/how/concepts/README.md docs/when/ROADMAP.md` — zero matches
- `grep -n "^### [1-5]\." docs/how/concept-design-review.md` — exactly 5 steps
- `grep "## Acceptance Gate" docs/how/concept-design-review.md` — match found
- `grep "Template-Compliance Audit Checklist" docs/how/DOCUMENTATION_PRINCIPLES.md` — match found
- `grep "DECISION_PROCESS" README.md` — match found
- Both process doc statuses verified Accepted
</verification>

<success_criteria>
PROC-01, PROC-02, PROC-04, PROC-05 satisfied. concept-design-review.md fully transformed with collapsed pipeline and acceptance gate. All stale cross-references fixed. Process docs finalized.
</success_criteria>

<output>
Create `.planning/phases/01.1-foundation-completion-consolidate-vision-lock-design-princip/01.1-03-SUMMARY.md` when done.
</output>
